# [ВИКОНАНО] План впровадження контекстного меню джерел, тестування з'єднання та копіювання URL

Цей план впровадження описує зміни для реалізації таких вимог:
1. Заміна нижнього спливаючого меню (синенького `PopupList`) джерела у налаштуваннях на праве бічне меню (`Sidebar(..., Sidebar::Side::RIGHT)`).
2. Додавання функції тестування мережевого підключення ("Test Connection") як у контекстне меню джерела, так і безпосередньо у меню редагування джерела `SourceEditMenu`.
3. Автоматичне копіювання назви джерела в поле URL, якщо назва схожа на URL-адресу, при його інтерактивному додаванні.

## Необхідний розгляд користувачем

> [!IMPORTANT]
> Тестування SMB-підключення вимагатиме наявності активованого макросу `BUILD_SMB2`. Для HTTP, WebDAV та FTP буде використовуватись бібліотека `curl` через вбудований метод `ToMemory`.

## Запитання

Немає.

## Пропоновані зміни (Усі успішно виконано)

---

### [Component: UI Settings (Sources & Network)]

#### [MODIFY] [settings_menu.cpp](file:///d:/git/dev/sphaira/sphaira/source/ui/menus/settings_menu.cpp) — **ВИКОНАНО**
- **IsUrlLike**: Додати допоміжну функцію для перевірки схожості рядка на URL.
- **BuildSourcesCategoryItems**:
  - Замінити `PopupList` у контекстному меню джерела (подія START) на `Sidebar` з розташуванням `Sidebar::Side::RIGHT`.
  - Додати пункти: "Enter/Connect", "Edit", "Test Connection", "Rename", "Properties", "Delete".
  - Реалізувати функцію тестування з'єднання:
    - Для SMB: створювати тимчасовий об'єкт `CSMB2FS` та викликати `CheckConnection()`.
    - Для інших (HTTP/WebDAV/FTP): надсилати запит `ToMemory(e)` з опцією `Flag_NoBody` (і методом `NLST` для FTP або `PROPFIND` для HTTP) і перевіряти успішність виконання.
    - Відображати хід перевірки за допомогою `ProgressBox` з назвою "Testing Connection...", та виводити результат через `App::Notify` або `OptionBox` у разі помилки.
- **SourceEditMenu::BuildEditItems**:
  - Додати в кінець списку налаштувань пункт "Test Connection" з аналогічною логікою тестування поточних налаштувань.

---

### [Component: File Browser Operations]

#### [MODIFY] [filebrowser.cpp](file:///d:/git/dev/sphaira/sphaira/source/ui/menus/filebrowser.cpp) — **ВИКОНАНО**
- **AddNetworkLocationInteractive**:
  - Додати перевірку введеної назви джерела через `IsUrlLike`.
  - Якщо введена назва є URL-подібною, автоматично копіювати її в поле `url` новостворюваного джерела, забезпечуючи наявність правильної схеми відповідно до обраного протоколу (наприклад, `smb://`, `webdav://`, `ftp://`, `http://`).

---

### [Component: CMake Configuration]

#### [MODIFY] [CMakeLists.txt](file:///d:/git/dev/sphaira/sphaira/CMakeLists.txt) (у підпапці `sphaira`) — **ВИКОНАНО**
- Ітерувати версію програми `sphaira_VERSION` з `0.13.230` до `0.13.231`.

## План верифікації — **ВИКОНАНО**

### Автоматичні тести
- Автоматичні тести відсутні.

### Ручна перевірка
1. Запустити збірку у WSL. (Збірка успішна, сформовано nro).
2. Перейти у розділ settings -> Sources.
3. Додати нове джерело з URL-подібною назвою (наприклад, `192.168.1.100/share`) та перевірити, що URL джерела автоматично заповнився як `smb://192.168.1.100/share`.
4. Натиснути START на джерелі та переконатися, що меню опцій виїжджає справа (`Sidebar`).
5. Вибрати пункт "Test Connection" та перевірити результат тесту.
6. Зайти у редагування джерела ("Edit") і перевірити наявність та працездатність пункту "Test Connection" в самому кінці списку налаштувань.
