# Актуальний канонічний план

Цей файл відновлено після повного аудиту Git-історії `plan*`, `task*`,
`REFACTOR_PLAN*`, `implementation_plan.md` та файлів у `archive/`.

## Правило цілісності

- Кожна задача має стабільний ID і посилання на джерело в історії.
- Виконані задачі не видаляються: їх переносять до `archive/task_archive.md` зі
  статусом, комітом і коротким доказом.
- Відкладені або заблоковані задачі не архівуються як виконані.
- Ручна перевірка на Switch не прирівнюється до автоматично підтвердженої.

## Стан пакета v0.13.241

Пакет підготовлено 2026-07-16. Історичний аудит `plan*`, `task*` та архівів
збережено; цей delivery завершує DBI-подібний інформаційний екран Games.

Завершено в цьому пакеті:

- відновлено загублені історичні задачі зі стабільними `HIST-*` ID;
- зміцнено HTTP/devoptab: безпечний GET retry/resume, перевірка truncation і
  фінального flush, коректна обробка EOF та локальних I/O-помилок;
- додано DBI-подібний Game Details з реальними NCM компонентами, tickets,
  saves, повним переліком мов і точковим NSP dump;
- додано L/R перехід між іграми, ZL/ZR вкладки, Base/Update/DLC/LayeredFS
  badges, безпечний viewport та підсвічення розміру гри у NAND/SD bars;
- у v0.13.240 підвищено контраст badges, додано `Без контенту`, X/Y-вибір як у Saves,
  групові операції лише за спільними можливостями вибраних ігор, виразні вкладки та об'єднану
  легенду `L/R` і `ZL/ZR`; `LayeredFS absent` замінено поясненням про папку модів Atmosphere;
- у v0.13.241 редактор мережевого джерела дозволяє змінити протокол SMB/WebDAV/FTP/HTTP,
  перебудовує набір полів і очищає несумісні приховані реквізити старого протоколу;
- додано Runtime Mode UX, Applet-профіль вебсервера, listener diagnostics,
  loopback self-test та добровільний Title Mode forwarder;
- повністю вилучено аудіо-runtime, BGM, системні звуки та `libpulsar`;
- автентифікацію foreground-вебсервера свідомо не додано за рішенням власника.

Поточний пріоритет: `HIST-61B` → `HIST-61C` → `HIST-60`. DBI-задачі
`HIST-54..56` виконуються після Games-блоку,
якщо не з'явиться критична регресія.

## Definition of Done

Задача може бути перенесена в архів лише коли:

1. реалізація відповідає критеріям у цьому файлі;
2. Release-збірка проходить;
3. автоматичні перевірки записані в `tests.md`;
4. апаратні сценарії або пройдені, або явно залишені `[ ]` як manual gate;
5. `task.md` і `archive/task_archive.md` оновлені в тому самому коміті.

## Активний roadmap

### HIST-54 — DBI: графік швидкості та загальний прогрес

Джерело: `task.md` перед `ba1442b`, id 54.

- На `State::Installing` замінити одиночний текст швидкості історією R/W.
- Додати другий progress bar для всієї вибраної черги, не лише поточного файла.

### HIST-55 — DBI: один динамічний рядок журналу на пакет

Джерело: `task.md` перед `ba1442b`, id 55.

- Один запис пакета має змінювати стан `Starting → Installed/Failed/Cancelled`,
  а не додавати окремий рядок для кожної події.
- Зберегти ручний scroll і follow-tail поведінку.

### HIST-56 — DBI: наочний ReviewQueue

Джерело: `task.md` перед `ba1442b`, id 56.

- Посилити візуальну ієрархію назви, install size та target.
- Додати сегментовані діаграми NAND/SD: базово зайняте місце плюс вибрані
  пакети, з підсвіченням сегмента поточного рядка.
- Перераховувати сегменти після X/R3 без миготіння та overflow.

### HIST-60 — Games: NAND ↔ SD, storage bars і сортування

Джерело: `task.md` перед `ba1442b`, id 60.

- Визначити безпечний офіційний API переносу встановленого NCM content.
- Підтримати mixed storage для Base/Update/DLC.
- Додати сортування за носієм, перевірку місця, прогрес і storage bars.

### HIST-61 — Games: інформаційний екран по A

Джерело: `task.md` перед `ba1442b`, id 61.

- A відкриває інформацію про гру замість негайного запуску.
- Показати Title ID, видавця, версію та Base/Update/DLC із розмірами/носіями.
- Зберегти окремий явний шлях запуску гри.

#### HIST-61A — Overview і компоненти

- Окремий Game Details у стилі Sphaira: обкладинка, назва, видавець, Title ID,
  display version, підтримувані мови та наявність LayeredFS.
- Вкладка Components: Base, Update, DLC і DataPatch з version, storage, size,
  content count та ticket/rights status.
- Вкладки Tickets і Saves без повторного сканування даних у `Draw()`.
- Реалізовано 2026-07-16: Details відкривається по A, читає реальні NCM записи,
  показує часткові помилки замість тихого пропуску, а dump із Details завжди
  обмежений поточною грою й перевіряє помилки формування NSP.
- Завершено у v0.13.239: спільний DBI-подібний overview, повний перелік мов,
  Content/Tickets/Saves, common/personalized/missing ticket status, фактичний
  allocated save-data size, L/R зміна гри та збереження focus у бібліотеці.

#### HIST-61B — безпечні операції над контентом

- Контекстні дії для конкретного компонента: dump NSP, integrity verification,
  content information та, після окремої перевірки NCM API, NAND ↔ SD move.
- Mount/Open content на першому етапі робити read-only; будь-який запис потребує
  підтвердження і повторної перевірки вільного місця.
- Delete, reset required version і force language винести в Advanced із чітким
  описом наслідків та можливістю скасування.

#### HIST-61C — saves, tickets і DLC

- Показувати всі saves гри за користувачами, тип, використаний/доступний розмір;
  відкривати наявний Save Manager для backup/restore замість дублювання логіки.
- Показувати rights IDs, тип ticket і key generation без відкриття секретних
  ключів у звичайному UI.
- DLC unlocker реалізовувати лише після окремої перевірки формату, сумісності та
  безпечного rollback; не позначати готовим лише через наявність dump ticket code.

### HIST-62 — Games: Base/Update/DLC/LayeredFS badges

Джерело: `task.md` перед `ba1442b`, id 62.

- Кешувати прапорці під час сканування бібліотеки, не в `Draw()`.
- Показувати компактні badges без перекриття назви та focus outline.
- LayeredFS визначати за актуальними Atmosphere contents paths.
- Завершено у v0.13.239: summary кешується по одному видимому запису за кадр,
  badges малюються на image rect, а focus/header/footer не перекриваються.

### HIST-GAMES-STORAGE — розмір вибраної гри у status bars

- Завершено у v0.13.239: NCM content сумується окремо для BuiltInUser та SD;
  у відповідному NAND/SD bar контрастним сегментом показується частка поточної
  гри. Для mixed-storage гри підсвічуються обидва носії.

## P2 — Непідтверджена сумісність і відкладені можливості

### HIST-USB-COMPAT — ручна матриця Tinfoil/DBI

Джерело: розділ 7 `archive/plan_archive.md`.

- Реальний Switch: ns-usbloader, fluffy, оригінальний DBI backend і
  dbibackend-qt; один/кілька NSP, cancel та повторний install.
- Це апаратна перевірка, а не відсутня реалізація коду.

### HIST-NFS-SFTP — NFS/SFTP network sources

Джерело: втрачений розділ 8.2, згаданий у `archive/plan_archive.md:364`.

SMB, WebDAV, FTP та HTTP мають реалізацію. NFS/SFTP не реалізовані; завдання
залишається окремим майбутнім розширенням після стабілізації curl-devoptab.

### HIST-PLAYER — вбудований медіаплеєр

Джерело: втрачений розділ 8.3, згаданий у `archive/plan_archive.md:365`.

Статус: **заморожено**. Не починати без окремого погодження; поточний шлях
`Play with NXMP` залишається підтримуваним рішенням.
Вбудований audio runtime і фонова музика вилучені 2026-07-16; якщо колись буде
погоджено плеєр, він має додаватися окремо, без автоматичного повернення BGM.

### HIST-WEB-APPLET — вебсервер у Applet/Title Mode

- Зберегти спробу запуску в Applet Mode з меншими socket buffers, але показувати
  користувачу обмеження пам'яті та інструкцію Title takeover: утримувати R під
  час запуску встановленої гри, дочекатися hbmenu/Kefir Hub і запустити Sphaira.
- Додати добровільне встановлення forwarder поточного NRO, якщо користувач не має
  зручного Title Mode entry point. Не встановлювати NSP без підтвердження.
- Автозапуск після install дозволити лише коли інсталятор надійно повертає точний
  application ID створеного forwarder; до цього запускати його з HOME вручну.
- Додати діагностику URL/IP/port і відрізняти помилку старту listener від Wi-Fi
  client isolation або недоступності консолі з іншого пристрою.
- Реалізовано 2026-07-16: видима довідка Runtime Mode, пункт-заміна для
  недоступного в Applet системного браузера, попередження про NSO scan fallback,
  інструкція на екрані сервера, журнал socket/bind/listen і два worker-и замість
  трьох у Applet, а локальний HTTP loopback self-test відрізняє справний listener
  від зовнішньої мережевої проблеми. Залишилася ручна перевірка client isolation.
- Автентифікацію не планувати: за рішенням власника сервер працює лише на явно
  відкритому foreground-екрані. Це рішення не скасовує валідацію шляхів/запитів.

## Виконані, але раніше втрачені при архівації

- HIST-HTTP-RETRY — обмежені повторні спроби файлових HTTP-завантажень,
  докачування з записаного offset і безпечний restart, якщо сервер не підтримує
  Range; реалізовано та перевірено Release-збіркою 2026-07-16.
- HIST-44 — стани/описи Module Manager: виконано у v0.13.213.
- HIST-46 — геометрія Module Manager: виконано у v0.13.213.
- HIST-47 — WebDAV settings folder: первісна standalone-модель замінена єдиним
  реєстром Sources; папка WebDAV тепер вибирає Sync Location. Реалізовано у
  v0.13.209 та остаточно узгоджено у v0.13.222.
- HIST-48 — touch viewport головного Settings: виконано у v0.13.213.

Деталі та докази перенесено до `archive/task_archive.md`.
