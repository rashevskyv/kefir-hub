# Поточні задачі

- [x] Рефакторинг cheats_menu.cpp (Фаза 1):
    - [x] Крок 1.1: Виділення cheats/cheats_dmnt.hpp/.cpp (Helpers dmnt/dmntcht) <!-- id: r1.1 -->
    - [x] Крок 1.2: Виділення cheats/cheats_lookup.hpp/.cpp (InstalledNcaLookupResult, BuildIdLookupResult) <!-- id: r1.2 -->
    - [x] Крок 1.3: Виділення cheats/cheats_db.hpp/.cpp (CachedCheatMetadata, NxDbVersionInfo) <!-- id: r1.3 -->
    - [x] Крок 1.4: Виділення cheats/cheat_files_menu.hpp/.cpp (CheatFilesMenu, CheatContentMenu, CheatCodeViewerMenu) <!-- id: r1.4 -->
    - [x] Крок 1.5: Виділення cheats/cheat_game_select_menu.hpp/.cpp (CheatGameSelectMenu, CheatDownloadMenu) <!-- id: r1.5 -->
    - [x] Крок 1.6: Фінальне очищення cheats_menu.cpp та верифікація збірки <!-- id: r1.6 -->
- [x] Рефакторинг settings_menu.cpp (Фаза 2):
    - [x] Крок 2.1: Виділення settings/settings_fs_utils.hpp/.cpp (Файлові та рядкові утиліти) <!-- id: r2.1 -->
    - [x] Крок 2.2: Виділення settings/settings_translations.hpp/.cpp (Переклади та завантаження) <!-- id: r2.2 -->
    - [x] Крок 2.3: Виділення settings/settings_tweaks.hpp/.cpp (Налаштування системи/розгін/пресети вентилятора) <!-- id: r2.3 -->
    - [x] Крок 2.4: Фінальне очищення settings_menu.cpp та верифікація збірки <!-- id: r2.4 -->
- [x] Рефакторинг web.cpp (Фаза 3):
    - [x] Крок 3.1: Виділення HTML/JS констант (source/web_pages.hpp) <!-- id: r3.1 -->
    - [x] Крок 3.2: Виділення класу QrCode (source/web_qr.hpp/.cpp) <!-- id: r3.2 -->
    - [x] Крок 3.3: Виділення UploadState та SocketStream у web_upload.hpp/.cpp <!-- id: r3.3 -->
- [x] Рефакторинг save_menu.cpp (Фаза 4):
    - [x] Крок 4.1: Виділення допоміжних функцій шляхів та імен бекапів у ui/menus/save/save_paths.hpp/.cpp <!-- id: r4.1 -->
    - [x] Крок 4.2: Виділення функцій WebDAV та локацій у ui/menus/save/save_locations.hpp/.cpp <!-- id: r4.2 -->
    - [x] Крок 4.3: Очищення save_menu.cpp та оновлення CMakeLists.txt <!-- id: r4.3 -->
- [x] Рефакторинг kefir_menu.cpp (Фаза 5):
    - [x] Крок 5.1: Виділення kefir/kefir_changelog.hpp/.cpp (ChangelogSegment та хелпери списків змін) <!-- id: r5.1 -->
    - [x] Крок 5.2: Виділення kefir/kefir_firmware.hpp/.cpp (Валідація та версії прошивки) <!-- id: r5.2 -->
    - [x] Крок 5.3: Виділення спільного віджета HoldConfirmBox <!-- id: r5.3 -->
- [x] Рефакторинг filebrowser.cpp (Фаза 6):
    - [x] Крок 6.1: Виділення хелперів типів файлів та ROM у filebrowser_assoc.hpp/.cpp <!-- id: r6.1 -->
    - [x] Крок 6.2: Виділення форми створення форвардерів ForwarderForm у filebrowser_forwarder.hpp/.cpp <!-- id: r6.2 -->
    - [x] Крок 6.3: Винесення важких операцій FsView у filebrowser_ops.cpp <!-- id: r6.3 -->
- [x] Рефакторинг app.cpp (Фаза 7):
    - [x] Крок 7.1: Винесення налаштувань та супутніх методів з app.cpp до source/app_settings.cpp <!-- id: r7.1 -->
    - [x] Крок 7.2: Винесення роботи з темами до source/app_theme.cpp <!-- id: r7.2 -->
    - [x] Крок 7.3: Фінальне очищення app.cpp та верифікація збірки <!-- id: r7.3 -->
- [x] Рефакторинг devoptab_common.cpp (Фаза 8):
    - [x] Крок 8.1: Винесення BufferedData та LruBufferedData до source/utils/devoptab_buffered.hpp/.cpp <!-- id: r8.1 -->
    - [x] Крок 8.2: Винесення PushPullThreadData, PushThreadData, PullThreadData до source/utils/devoptab_curl_thread.hpp/.cpp <!-- id: r8.2 -->
    - [x] Крок 8.3: Винесення MountCurlDevice до source/utils/devoptab_curl_device.hpp/.cpp <!-- id: r8.3 -->
- [x] Рефакторинг settings_menu.cpp (Фаза 9):
    - [x] Крок 9.1: Виділення settings/settings_fancurve.hpp/.cpp (Runtime-частина: EvaluateFanPercent, FanCurveSensorSample, FanCurveSensorReader, SphairaFanState) <!-- id: r9.1 -->
    - [x] Крок 9.2: Виділення settings_fancurve.cpp (Малювальні хелпери fan-curve) <!-- id: r9.2 -->
    - [x] Крок 9.3: Винесення FanCurveMenu до settings_fancurve.hpp/.cpp <!-- id: r9.3 -->
    - [x] Крок 9.4: Очищення settings_menu.cpp та верифікація збірки <!-- id: r9.4 -->
- [x] Рефакторинг save_menu.cpp (Фаза 10):
    - [x] Крок 10.1: Виділення ui/menus/save/save_menu_ops.cpp (Тіла операційних методів) <!-- id: r10.1 -->
    - [x] Крок 10.2: Очищення save_menu.cpp та верифікація збірки <!-- id: r10.2 -->
    - [x] Виправлення дефектів: Відновлено 3 втрачених коментарі в save_menu_detail.cpp <!-- id: r10.def -->
- [x] Рефакторинг cheats/cheat_game_select_menu.cpp (Фаза 11):
    - [x] Крок 11.1: Виділення класу CheatDownloadMenu у новий модуль cheat_download_menu.hpp/.cpp <!-- id: r11.1 -->
    - [x] Крок 11.2: Розбір анонімних та detail хелперів <!-- id: r11.2 -->
    - [x] Крок 11.3: Очищення cheat_game_select_menu.cpp та верифікація збірки <!-- id: r11.3 -->
    - [x] Виправлення дефектів: Відновлено 3 втрачених коментарі в cheat_download_menu.hpp/cpp <!-- id: r11.def -->
- [x] Рефакторинг web.cpp (Фаза 12):
    - [x] Крок 12.1: Виділення HTTP та строкових примітивів у web_http.hpp/.cpp <!-- id: r12.1 -->
    - [x] Крок 12.2: Виділення логіки галереї скріншотів у web_screenshots.hpp/.cpp <!-- id: r12.2 -->
    - [x] Крок 12.3: Очищення web.cpp та верифікація збірки <!-- id: r12.3 -->
    - [x] Виправлення дефектів: видалено IsImagePath/JsonEscape з web.cpp, відновлено LF закінчення рядків <!-- id: r12.def -->
- [x] Рефакторинг app.cpp (Фаза 13):
    - [x] Крок 13.1: Виділення методів відображення налаштувань у новий файл app_display_options.cpp <!-- id: r13.1 -->
- [x] Рефакторинг та дедуплікація (Фаза 14):
    - [x] Крок 14.1: Оновлення статусу HoldConfirmBox (виконано в bd31eae) <!-- id: r14.1 -->
    - [x] Крок 14.2: Дедуплікація hexIdToStr та HashStr <!-- id: r14.2 -->
    - [x] Крок 14.3: Об'єднання функцій Trim <!-- id: r14.3 -->
    - [x] Крок 14.4: Заміна DirectoryExists на fs::DirExists <!-- id: r14.4 -->
    - [x] Крок 14.5: Пропуск URL/HTML декодування (задокументовано відмінності) <!-- id: r14.5 -->


- [x] Вивчити наявний стан та спланувати синхронізацію сейвів з віддаленим джерелом (WebDAV) <!-- id: 0 -->
- [x] Реалізувати лістинг віддаленої теки WebDAV (PROPFIND XML парсер / рядковий пошук) <!-- id: 1 -->
- [x] Додати синхронізацію сейвів (SyncPlan, UI в Save Menu, опція Auto-sync) <!-- id: 2 -->
- [x] Перевірити сумісність та збірку через WSL <!-- id: 3 -->
- [x] Створити новий тег/реліз на GitHub з ченджлогом та оновити документацію/README.md <!-- id: 4 -->
- [x] Реалізувати налаштування пріоритету місця встановлення та резервування місця (пункт 3.3) <!-- id: 5 -->
- [x] Виправити верстку статус-бару (наклейка смужок на час/заряд, вирівнювання літер D) (пункт 10) <!-- id: 6 -->
- [x] Виправити переклади тултіпів сортування та "Show Hidden" (пункт 11.1) <!-- id: 7 -->
- [x] Реорганізувати меню Software -> DBI (пункт 11.2) <!-- id: 8 -->
- [x] Об'єднати та перекласти легенди Themezer і Screenshot (пункт 11.3) <!-- id: 9 -->
- [x] Реалізувати прибирання тем з Favorites по R3 та оновити легенду (пункт 11.4) <!-- id: 10 -->
- [x] Виправити відображення зарядки батареї (малювання блискавки лініями, пульсуючий відсоток заряду) (пункт 10.3) — скасовано, поточний вигляд влаштовує <!-- id: 11 -->
- [x] Додати налаштування WebDAV (URL, User, Pass) у Network settings та інтегрувати з автосинхронізацією сейвів (пункт 12) <!-- id: 12 -->
    - **Контекст (де що лежить):**
        - Зараз WebDAV-адреси для синку сейвів беруться з `/config/sphaira/locations.ini` через `location::Load()` (`sphaira/source/location.cpp:12,42`; структура `location::Entry {name, url, user, pass, bearer, pub_key, priv_key, port}` — `sphaira/include/location.hpp:12`).
        - Фільтр "що вважати WebDAV" (url починається з `webdav://`/`http://`/`https://`) продубльовано у **двох** місцях `sphaira/source/ui/menus/save_menu.cpp`: ~1420–1430 (автосинк після бекапу) і ~1860–1895 (ручний синк `SyncSavesRemote`).
        - Секція "Network" у налаштуваннях: `sphaira/source/ui/menus/settings_menu.cpp:2349–2359` — масив `SettingsItem{label, description, value_fn, action_fn}`; хелпери `MakeBoolItem`/`MakeOptionItem` там же (~1305–1330).
        - Текстовий ввід: `swkbd::ShowText(std::string& out, guide, initial)` — `sphaira/include/swkbd.hpp:8`.
        - Патерн зберігання опцій: `option::OptionString m_theme_path{INI_SECTION, "theme", ...}` в `sphaira/include/app.hpp:309` + статичні геттери/сеттери в `app.cpp` (зразок — `App::SetMtpEnable`, `app.cpp:921`).
    - **Кроки:**
        1. `app.hpp`: додати `option::OptionString m_webdav_url{INI_SECTION, "webdav_url", ""};`, аналогічно `m_webdav_user`, `m_webdav_pass`. Додати статичні `App::GetWebdavUrl/SetWebdavUrl` (+ user/pass) за наявним патерном.
        2. `settings_menu.cpp`, секція "Network": три нових `SettingsItem` — "WebDAV URL", "WebDAV User", "WebDAV Password". value_fn: URL і User показувати як є, пароль маскувати зірочками (`std::string(n, '*')`). action_fn: `swkbd::ShowText` з initial = поточне значення; по успіху `App::Set...`. Порожній URL = функція вимкнена.
        3. `save_menu.cpp`: винести дубльований збір локацій у хелпер `auto GetWebdavLocations() -> std::vector<location::Entry>`: (а) якщо `App::GetWebdavUrl()` не порожній — першим додати `Entry{.name="WebDAV (Settings)", .url=..., .user=..., .pass=...}`; (б) далі записи з `location::Load()` за старим фільтром, пропускаючи дублікати по url. Замінити обидва місця (~1420 і ~1862) на виклик хелпера.
        4. i18n: нові рядки в `assets/romfs/i18n/en.json` і `uk.json` (інші мови — англійський текст).
        5. Підняти версію в `sphaira/CMakeLists.txt`, додати запис у `walkthrough.md`.
    - **Перевірка:** без заповненого URL поведінка не змінюється (locations.ini працює як раніше); із заповненим URL автосинк і ручний синк працюють без locations.ini; пароль у меню не відображається відкритим текстом.
    - **Зроблено у v0.13.146:** додано збережувані URL/User/Password у Network settings; пароль маскується; ручний та автоматичний sync використовують спільний список WebDAV-локацій із пріоритетом налаштувань і дедуплікацією `locations.ini` за URL. Після рев’ю додано trim, канонізацію адреси без схеми та `https://` у `webdav://` для MKCOL, нормалізацію хвостового `/` і безпечнішу фіксовану маску пароля. Контрольна WSL-збірка `ReleaseWithInstall` пройшла успішно (`sphaira_nro`, exit code 0).
- [x] Додати пункт "Games" in меню "Tools" (пункт 13) <!-- id: 13 -->
    - **Контекст:**
        - Меню Games вже існує: `ui::menu::game::Menu` (`sphaira/source/ui/menus/game_menu.cpp:440`, конструктор приймає `u32 flags`); з головного меню воно вже відкривається (`main_menu.cpp:45`). Треба лише додати плитку в Tools.
        - Плитки Tools: `sphaira/source/ui/menus/tools_menu.cpp:95–121` (вектор `m_items`), іконки — `#embed <icons/*.png>` (рядки 29–59), завантаження у `LoadIcons()` з жорсткими індексами `m_items[0..7]` (рядки 232–239).
        - Готова іконка є: `assets/embed/icons/game-hub.png`.
    - **Кроки:**
        1. `tools_menu.cpp`: додати `#include "ui/menus/game_menu.hpp"` та `constexpr const u8 ICON_GAMES[]{ #embed <icons/game-hub.png> };`.
        2. Вставити у `m_items` після "Saves" (тобто на позицію 5): `{ "Games"_i18n, "View, launch and manage installed games."_i18n, 0, [](){ App::Push<ui::menu::game::Menu>(MenuFlag_None); }}`.
        3. Оновити `LoadIcons()`: після вставки всі індекси нижче зсунуться — виправити всі (`m_items[5]` = ICON_GAMES, далі software/themes/settings на 6/7/8).
        4. Сітка `List(3, 9, ...)` (tools_menu.cpp:134) показує 9 елементів — стане 9 плиток, влазить без скролу; якщо разом із задачею "Module Manager" стане 10 — перевірити, що грід скролиться коректно (List із GRID layout це підтримує).
        5. i18n (en + uk), версія в `CMakeLists.txt`, запис у `walkthrough.md`.
    - **Перевірка:** плитка з іконкою відкриває той самий список ігор, що й з головного меню; фокус/скрол у гріді не ламається.
    - **Порада:** виконувати разом із задачею id 15 (обидві правлять tools_menu.cpp і LoadIcons) — один прохід, один зсув індексів.
    - **Зроблено у v0.13.143:** у `tools_menu.cpp` додано плитку "Games" після "Saves", підключено `ui::menu::game::Menu`, використано іконку `game-hub.png`; `LoadIcons()` переведено з жорстких індексів на масив іконок у порядку `m_items`.
- [x] Виправити чутливість до регістру при пошуку NCA (помилка YatiNcaNotFound) (пункт 11.5) <!-- id: 14 -->
- [x] Перенести керування модулями (Component Manager) у меню Tools та перейменувати на Module Manager (пункт 14) <!-- id: 15 -->
    - **Контекст:**
        - Пункт "Component Manager" зараз у Settings → Homebrew: `sphaira/source/ui/menus/settings_menu.cpp:2370–2372`, пушить `ui::menu::hats::UninstallerMenu`.
        - Заголовок самого меню: `sphaira/source/ui/menus/uninstaller_menu.cpp:109` — `MenuBase{"Component Manager", MenuFlag_None}`; `GetShortTitle()` → `"Components"` (`sphaira/include/ui/menus/uninstaller_menu.hpp:31`).
        - Готова іконка є: `assets/embed/icons/component-manager.png`.
    - **Кроки:**
        1. `tools_menu.cpp`: `#include "ui/menus/uninstaller_menu.hpp"`; додати `ICON_MODULE_MANAGER` (`#embed <icons/component-manager.png>`); нова плитка `{ "Module Manager"_i18n, "Start, stop and configure installed sysmodules."_i18n, 0, [](){ App::Push<ui::menu::hats::UninstallerMenu>(); }}` (розумна позиція — поруч із "Kefir Settings"/"Cheats"); оновити індекси в `LoadIcons()`.
        2. `settings_menu.cpp`: видалити пункт "Component Manager" із секції Homebrew (рядки 2370–2372).
        3. `uninstaller_menu.cpp:109`: заголовок → `"Module Manager"_i18n`; у hpp `GetShortTitle()` → `"Modules"`.
        4. Пройтись `grep -rn "Component Manager"` по `sphaira/` та `assets/romfs/i18n/` — оновити/додати ключ "Module Manager" у en.json і uk.json (старий ключ можна лишити, він просто перестане вживатись).
        5. Версія в `CMakeLists.txt`, запис у `walkthrough.md`.
    - **Перевірка:** плитка в Tools відкриває колишній Component Manager із новим заголовком "Module Manager"; у Settings → Homebrew пункта більше немає; переклади підхоплюються.
    - **Порада:** виконувати разом із задачею id 13 (спільний файл tools_menu.cpp).
    - **Зроблено у v0.13.143, уточнено у v0.13.144/v0.13.145:** спочатку `tools_menu.cpp` отримав плитку "Module Manager", потім пункт перенесено в `Kefir Settings`. У v0.13.145 прибрано manifest/components модель із користувацького меню: `UninstallerMenu` тепер сканує реальні sysmodules за `/atmosphere/contents/*/toolbox.json`, показує running/autostart/reboot-required; A керує тільки runtime-станом dynamic-модулів, Y окремо керує `boot2.flag`. Після рев'ю виправлено UB у `ParseProgramId`, прибрано побічну зміну autostart з кнопки A, уточнено опис без remove, додано i18n `"Toggle"` та прибрано дубльований `UpdateActions()`.
- [x] Продумати розміщення та перейменувати "DBI Install" у "PC Install (USB)" (пункт 15) <!-- id: 16 -->
- [x] Переробити виклик Web Server на вибір через бокове меню (Web Server або PC Install) по кнопці Plus в Tools (пункт 15.1) <!-- id: 17 -->
- [x] Переналаштувати навігацію сторінок на L/R, стрибки на ZL/ZR, перемістити номери сторінок вгору в Themezer та оновити переклади (пункт 11.3.1) <!-- id: 18 -->
- [x] Реорганізувати контекстне меню файлового менеджера, перекласти всі описи та перейменувати "Sort by" на "View" (пункт 16) <!-- id: 19 -->
- [x] Виправити помилку компіляції файлового менеджера (пункт 16.1) <!-- id: 20 -->
- [x] Виправити падіння Themezer при виході, приховати ZL/ZR з легенди та додати автоперехід сторінок на Down/Right (пункт 11.3.2) <!-- id: 21 -->
- [x] Закомітити інші незавершені зміни (WebDAV автосинхронізація, оновлення індикатора батареї, чистка коду) (пункт 17) <!-- id: 22 -->
- [x] Реорганізувати порядок опцій файлу, додати стрілочки вглиб до підменю Sidebar та оновити версію (пункт 16.2) <!-- id: 23 -->
- [x] Виправити помилку зайвої фігурної дужки в filebrowser.cpp (пункт 16.3) <!-- id: 24 -->
- [x] Переписати відображення стрілочок підменю на векторне малювання NanoVG, прибрати стрілочку з PC Install (USB) та оновити версію (пункт 16.4) <!-- id: 25 -->
- [x] Реорганізувати MTP: видалити DevNull та реалізувати встановлення/копіювання в корінь microSD (пункт 3.1) <!-- id: 26 -->
    - [x] Видалити FsDevNullProxy та очистити код SpeedTest і ProgressBox швидкості <!-- id: 27 -->
    - [x] Забезпечити, щоб в MTP ініціалізувалися лише microSD card, Image nand, Image sd та Install <!-- id: 28 -->
- [x] Прибрати ImageNand та ImageSD зі списку MTP-накопичувачів (пункт 18) <!-- id: 29 -->
- [x] Зробити встановлення поведінкою за замовченням при киданні файлів у корінь microSD по MTP: NSP/NSZ/XCI/XCZ — встановлення, NRO — у /switch, решта — копіювання в корінь; маршрутизація через розширювану таблицю правил ROOT_DROP_RULES (пункт 19) <!-- id: 30 -->
    - [x] Виправити відсутність перехоплення (файли лише копіювалися): libhaze передає кореневі шляхи microSD з подвійним слешем ("//file.nsz"), перевірку кореня переписано на підрахунок компонентів шляху (пункт 19.1) <!-- id: 32 -->
    - [x] Змінити маршрут NRO: класти в /switch/<ім'я додатку>/<ім'я додатку>.nro замість /switch/<ім'я>.nro (пункт 19.2) <!-- id: 33 -->
    - [x] Виправити дедлок м'ютекса при старті встановлення з кореня microSD (зависання копіювання в Explorer): on_thing() викликався під уже захопленим g_shared_data.mutex (пункт 19.3) <!-- id: 34 -->
    - [x] Виправити фантомний 0-байтовий файл та неробочий сценарій "Скопіювати та замінити" для перехоплених шляхів (пункт 19.4) <!-- id: 35 -->
    - [x] Виправити "Пристрій припинив відповідати" при встановленні великих файлів по MTP: OnInstallClose блокував єдиний потік MTP-респондера до завершення встановлення, Windows вважала пристрій завислим — прибрано очікування, встановлення триває у фоні (пункт 19.5) <!-- id: 36 -->
- [x] Додати в налаштування можливість вказувати, які папки відображаються в MTP та з якими назвами (пункт 20) <!-- id: 31 -->
    - **Контекст:**
        - Сховища MTP реєструються жорстко в `haze::Init()` (`sphaira/source/haze_helper.cpp:900–919`): `FsProxy(FsNativeSd, "", "microSD card")` та `FsInstallProxy("install", "Install (NSP, XCI, NSZ, XCZ)")`.
        - **КРИТИЧНО:** перший рядковий аргумент (`m_name`) — технічний префікс шляхів (`FixPath`, haze_helper.cpp:148–160); порожній name у microSD особливий (кореневі шляхи виду `//file.nsz`, див. `IsRootPath`). Налаштовувати можна ЛИШЕ `display_name` (другий аргумент), `m_name` не чіпати ніколи.
        - Перезапуск MTP: `App::SetMtpEnable(false)` → `App::SetMtpEnable(true)` (`app.cpp:921–931`) — робить `haze::Exit()`/`haze::Init()` + `RegisterMtpCallbacks()`.
        - Патерн опцій — як у задачі id 12 (option::OptionBool/OptionString в app.hpp).
    - **Кроки:**
        1. `app.hpp`: `option::OptionBool m_mtp_show_sd{INI_SECTION, "mtp_show_sd", true};`, `m_mtp_show_install{INI_SECTION, "mtp_show_install", true};`, `option::OptionString m_mtp_name_sd{INI_SECTION, "mtp_name_sd", ""};`, `m_mtp_name_install{...}` (порожньо = стандартна назва). Геттери/сеттери за патерном.
        2. `haze_helper.cpp` `Init()`: зробити маленьку таблицю сховищ (`struct MtpStorageDef { видимість, кастомна назва, фабрика прокси, стандартна назва }`) і реєструвати лише увімкнені; display_name = кастомна назва, якщо не порожня, інакше стандартна. Таблиця потрібна, щоб задача id 37 (диск Saves) додавалась одним рядком. Якщо всі сховища вимкнені — не стартувати haze: лог + `App::Notify` і `return false`.
        3. `settings_menu.cpp`, секція "Network": додати `SettingsItem` типу `SettingsItemKind::Folder` "MTP storages", який відкриває `Sidebar` (зразок сайдбара — `save_menu.cpp:721–737`): `SidebarEntryBool` "Show microSD card", "Show Install folder"; `SidebarEntryCallback` "microSD card name", "Install folder name" (через `swkbd::ShowText`, порожній ввід = скинути на стандартну).
        4. Після будь-якої зміни, якщо `App::GetMtpEnable()` — перезапустити: `SetMtpEnable(false); SetMtpEnable(true);` (інакше зміни підхопляться при наступному вмиканні).
        5. i18n (en + uk), версія, walkthrough.
    - **Перевірка:** вимкнення Install прибирає папку з провідника Windows після перепідключення; кастомна назва відображається; вимкнення обох — MTP не стартує з повідомленням; root-drop install на microSD далі працює (name не чіпали).
- [ ] Показувати розшифровані сейви як окремий МТП-диск (по аналогії з microSD/Install) (пункт 21) <!-- id: 37 -->
    - **Порядок:** робити ПІСЛЯ id 31 (таблиця сховищ MTP) і ПІСЛЯ id 38 (щоб ієрархія "гра → тип" збігалась із новою структурою бекапів). Найскладніша задача списку.
    - **Контекст:**
        - Базові класи прокси: `FsProxyBase` (`sphaira/source/haze_helper.cpp:143–172`), приклад повністю віртуального FS — `FsProxyVfs` (582–726), приклад реального — `FsProxy` (180–580, делегує в `fs::Fs`).
        - Маунт сейва: `fs::FsNativeSave(data_type, space_id, &attr, read_only)` (`sphaira/include/fs.hpp:535–547`); read_only → `fsOpenReadOnlySaveDataFileSystem`.
        - Перелік сейвів: `fsOpenSaveDataInfoReaderWithFilter` — робочий приклад `Menu::ReadSaveEntries` (`sphaira/source/ui/menus/save_menu.cpp:875–913`) + `GetFsSaveAttr` там же; імена ігор — через `title::Get(application_id)` (див. `Entry::GetName` у save_menu), санітизація імені — `BuildSaveName` (`save_menu.cpp:324–329`, `title::utilsReplaceIllegalCharacters`).
        - Акаунти: як save_menu заповнює `m_accounts` (пошук `accountListAllUsers`/`GetAccountName` у save_menu.cpp) — потрібно для підпапок Account-сейвів.
    - **Рішення:**
        1. Новий файл `sphaira/source/mtp_save_fs.cpp` (+ hpp у include) зі структурою `FsSaveProxy : FsProxyBase`, `name="saves"`, display "Saves (read-only)". `FsProxyBase` доведеться винести в спільний хедер (наприклад, `include/haze_proxy_base.hpp`) або оголосити прокси прямо в haze_helper.cpp — обрати менш інвазивне (простіше: додати клас у haze_helper.cpp поряд з існуючими, без нового файлу).
        2. Віртуальна ієрархія (2 рівні + вміст сейва): `/<GameName [TitleID]>/<Account <ім'я користувача> | BCAT | Device | Cache>/<файли змонтованого сейва>`. Суфікс `[TitleID]` додавати завжди — знімає колізії однакових назв; імена обрізати до розміру `FsDirectoryEntry::name`.
        3. При реєстрації (в `haze::Init()`): просканувати сейви у `FsSaveDataSpaceId_User` (типи Account/Bcat/Device/Cache, БЕЗ System) і побудувати мапу "компонент шляху → FsSaveDataInfo". Системні сейви не показувати (перша ітерація).
        4. Lazy mount: у кожній операції розбирати шлях — перші 2 компоненти визначають сейв; відкривати `FsNativeSave` read-only і кешувати в `std::map<ключ, std::unique_ptr<fs::FsNative>>` (ліміт ~4, LRU-закриття). Хвіст шляху делегувати у змонтований fs так само, як `FsProxy` делегує в `m_fs`.
        5. Каталоги рівнів 1–2 — повністю віртуальні: `GetEntryType` → Dir, `OpenDirectory`/`ReadDirectory` генерують списки з мапи (зразок генерації — `FsProxyVfs::ReadDirectory`).
        6. Усі write-операції (Create/Delete/Rename/Write/SetSize) → `R_THROW(FsError_NotImplemented)`. Запис у сейв вимагає commit і перевірок цілісності — окрема майбутня задача.
        7. `MultiThreadTransfer` → `false` (сейви малі, безпечніше однопотоково).
        8. Реєстрація через таблицю сховищ із задачі id 31: нова опція `m_mtp_show_saves{INI_SECTION, "mtp_show_saves", false}` (за замовченням вимкнено) + тумблер у Sidebar "MTP storages".
    - **Ризики/нюанси:** великі списки ігор — сканувати один раз при Init, не на кожен ReadDirectory; сейв, відкритий read-only, може не відкритись, якщо зайнятий — повертати помилку тільки для цього піддерева, не валити весь диск; після Exit() закривати всі закешовані маунти (деструктор прокси).
    - **Перевірка:** диск "Saves" видно у Windows поряд із microSD/Install; папки ігор → підпапки типів → файли сейва читаються/копіюються на ПК; запис у диск дає помилку без крашу; вимкнений тумблер — диска немає.
- [x] Показувати popup зі швидкістю/статусом і давати змогу згорнути його по L3, зберігаючи навігацію меню; попереджати (не блокувати) про Home під час активної передачі (пункт 23) <!-- id: 39 -->
    - [x] Зробити фонове встановлення (root-drop / папка Install по MTP) не-модальним: ProgressBox більше не блокує стек віджетів, кероване App::PushTransfer/m_active_transfer_pbox (пункт 23.1) <!-- id: 40 -->
    - [x] Додати згортання по L3: глобальна перевірка в App::Update() незалежно від активного віджета; в згорнутому вигляді — маленький бейдж у кутку, розгорнутому — повний діалог без затемнення фону і без блокування вводу (пункт 23.2) <!-- id: 41 -->
    - [x] Додати попередження про Home під час активної передачі: постійний текст на HUD (не блокуємо кнопку — Switch все одно призупиняє застосунок, справжнього "перехоплення до" немає) (пункт 23.3) <!-- id: 42 -->
    - [ ] Додати popup зі швидкістю/прогресом для звичайного (не-install) копіювання файлів по MTP — окремий менший підзавдання, ще не реалізовано (пункт 23.4) <!-- id: 43 -->
        - **Контекст:**
            - Події прогресу вже приходять: `haze_callback` (`sphaira/source/haze_helper.cpp:860–896`) отримує `ReadBegin/ReadProgress/ReadEnd` і `WriteBegin/WriteProgress/WriteEnd` з іменем файлу, offset та size — зараз лише логуються. Увага: колбек виконується на потоці haze, НЕ на головному.
            - Перехоплені install-файли вже мають власний попап (`install_stream_menu_base.cpp:376` через `App::PushTransfer`) — новий попап потрібен лише для звичайних копіювань.
            - Маршалінг у головний потік: `evman::push(evman::FunctionalEventData{...})` — зразок `install_stream_menu_base.cpp:341,370`, обробка в `app.cpp:492`.
            - Detached-ProgressBox: `App::PushTransfer` (`app.cpp:540`, лише один активний одночасно), потокобезпечні `NewTransferForce`/`UpdateTransferForce` (`include/ui/progress_box.hpp:41–42`); L3-мінімізація і HUD-попередження про Home вже глобальні для `m_active_transfer_pbox` — додаткових правок не треба.
            - ProgressBox обов'язково запускає воркер-потік з колбеком — для MTP колбек має просто чекати завершення: цикл `waitSingle(waiterForUEvent(&done_event), timeout)` з перевіркою `pbox->ShouldExit()`.
        - **Кроки:**
            1. `haze_helper.cpp`: стан UI-попапа — `Mutex`, `ProgressBox* pbox` (сирий вказівник, доступ лише під м'ютексом), `UEvent done_event`, `bool ui_alive`.
            2. На `WriteBegin`/`ReadBegin`: пропустити, якщо це install-потік (перевірка під `g_shared_data.mutex`: `in_progress` і збіг імені з `current_file`); інакше, якщо попап неактивний — `evman::push(FunctionalEventData)`, який на головному потоці створює detached ProgressBox ("Copying via MTP"_i18n), викликає `App::PushTransfer` і під м'ютексом записує вказівник + `ui_alive=true`. done-колбек попапа: під м'ютексом `pbox=nullptr; ui_alive=false` (захист від використання після знищення в `app.cpp:1129–1130`).
            3. На `*Progress`: під м'ютексом, якщо pbox живий — `NewTransferForce(ім'я файлу)` при зміні файлу і `UpdateTransferForce(e.progress.offset, e.progress.size)`.
            4. На `*End`: сигналити `done_event`. Проти "блимання" при серії дрібних файлів: воркер після сигналу чекає ще ~1.5 с — якщо за цей час прийшов новий Begin (скинути прапорець), продовжити цикл, інакше вийти. Мінімально допустима версія — закривати одразу.
            5. Врахувати, що `App::PushTransfer` ігнорує виклик, якщо вже є активний попап (наприклад, йде install) — у цьому разі просто не показувати (лог).
            6. Версія в `CMakeLists.txt`, запис у `walkthrough.md`.
        - **Перевірка:** копіювання великого файлу по MTP у звичайну папку показує попап зі швидкістю; L3 згортає; install-потік не породжує другий попап; читання файлів із консолі на ПК теж показує прогрес; після завершення попап зникає, при серії файлів не блимає.
- [ ] Зробити стани та описи в "Керуванні модулями" однозначними (пункт 14.1; після id 45) <!-- id: 44 -->
    - **Проблема:** поточний напис на кшталт `Увімкнено / Авто` змішує два незалежні значення: процес працює зараз і модуль стартуватиме після наступного перезапуску. Позначка `потрібне перезавантаження` також хибно звучить як вимога негайно перезавантажити консоль, хоча це лише властивість модуля: його стан застосовується під час старту системи.
    - **UX-рішення:** замінити складений статус двома явно підписаними рядками/бейджами: `Зараз: увімкнено|вимкнено` і `Після перезапуску: увімкнено|вимкнено`. Для модулів із `requires_reboot` показувати нейтральне `Застосовується після перезапуску`, а при A — пояснення `Цей модуль змінюється після перезапуску. Використайте автозапуск.` Це не означає, що користувач вже щось змінив або мусить перезапускати консоль.
    - **Описи модулів:** не використовувати коментарі в JSON: стандартний JSON не підтримує коментарі, тому вони ламатимуть сумісний парсер. Також не додавати опис у `toolbox.json`: цей файл належить модулю та може бути перезаписаний під час його оновлення. Після id 45 створити власний реєстр застосунку `<новий конфіг-корінь>/modules.json`, прив'язаний до TID: `{"00FF...": {"en": "...", "uk": "..."}}`. Менеджер читає `toolbox.json` тільки для виявлення модуля та його технічного стану, а опис бере з реєстру на SD. Власний реєстр можна поповнювати без зміни пакетів модулів; невідомі сторонні модулі лишаються сумісними й показують нейтральний текст про відсутність опису.
    - **Кроки:**
        1. Розширити `ModuleItem` полем `description`; додати `<новий конфіг-корінь>/modules.json` як реєстр описів за TID, завантажити його один раз під час відкриття меню та вибирати `en`/`uk` за поточною мовою. `ParseToolbox()` лишається відповідальним тільки за `tid`, `name` і `requires_reboot`.
        2. Переробити `Draw()` у `uninstaller_menu.cpp`: прибрати `On / Auto` та `Off / Manual`, вивести два окремо підписані стани; збільшити висоту рядка лише настільки, щоб підписані стани та назва не накладалися.
        3. Показувати опис вибраного модуля в підзаголовку меню; якщо опису немає — `Опис модуля не надано`. Program ID залишити як технічну довідку, але не підміняти ним пояснення для користувача.
        4. Перейменувати ` - reboot required` і повідомлення A відповідно до UX-рішення; для dynamic-модулів A як і раніше керує лише поточним процесом, Y — лише `boot2.flag`.
        5. Додати всі нові ключі в `en.json` і `uk.json`, створювати стартовий `modules.json` у новому конфіг-корені лише якщо його ще немає, підняти версію, доповнити `walkthrough.md`.
    - **Перевірка:** на екрані без пояснень зрозуміло, що `Зараз` не дорівнює `Після перезапуску`; натискання A не змінює другий рядок, Y не змінює перший; Mission Control показує властивість застосування після перезапуску, а не помилкову вимогу негайно перезавантажитися; FunControl має опис із реєстру в конфіг-папці, а перевстановлення FunControl не змінює цей опис; старі сторонні toolbox-файли не зникають зі списку.
- [x] Перебрендувати Sphaira як окремий інструмент Kefir і мігрувати конфігурацію (пункт 14.2) — реалізовано у v0.13.147 <!-- id: 45 -->
    - **Затверджено:** назва застосунку — **Kefir Hub**; єдиний конфіг-корінь на SD після міграції — `/config/kefir/`.
    - **Контекст:** теперішній `/config/sphaira/` містить не один файл, а `config.ini`, `playlog.ini`, `locations.ini`, `log.txt`, локальні i18n, асоціації, GitHub-джерела, пакети, завантаження й дані тем. Жорсткі шляхи є в `app.hpp`, `location.cpp`, `log.cpp`, `i18n.cpp`, `settings_menu.cpp`, `filebrowser.cpp`, `ghdl.cpp` та інших ділянках. Назва також формує NACP, вихідний `.nro`, веб-заголовки, README, GitHub-джерело та `assets/icon.jpg`.
    - **Кроки:**
        1. Винести корінь даних застосунку в одну константу; усі нові файли, включно з `modules.json`, будувати від неї.
        2. На першому запуску: якщо `/config/kefir/` ще немає, повністю перенести весь `/config/sphaira/` у новий корінь. Після успіху не читати стару папку: застосунок використовує лише `/config/kefir/`. Якщо міграція не завершилась — показати помилку й не змішувати два корені. У лог записати результат міграції.
        3. Замінити жорсткі шляхи й користувацькі назви Sphaira на новий бренд; технічні внутрішні назви C++/CMake можна лишити на першому етапі, щоб не змішувати ребрендинг із ризикованим масовим рефакторингом.
        4. Оновити NACP display name, назву та шлях вихідного NRO, іконку `assets/icon.jpg`, веб-заголовки, README, локалізації, release/artifact-імена й власні GitHub-джерела. Посилання на upstream Sphaira зберегти в Credits.
        5. Перевірити оновлення поверх старої інсталяції, чисту SD-карту, запуск як hbmenu replacement і прямий запуск NRO; підняти версію та описати міграцію у walkthrough.
    - **Перевірка:** старі налаштування, локації, переклади й теми з `/config/sphaira/` доступні після оновлення в `/config/kefir/`; нові зміни пишуться лише в `/config/kefir/`; на домашньому екрані, у веб-інтерфейсі та документації відображається нова назва й нова іконка; посилання на upstream не зникають.
    - **Реалізовано:** шляхи зведено в `app_paths.hpp`; старий корінь атомарно перейменовується до читання конфігів, а конфлікт двох коренів або помилка rename зупиняє запуск. Оновлено NACP, `kefir-hub.nro`, hbmenu identity/path, web, README, GitHub artifact/source names та en/uk. Обрана іконка — кремове коло з кольоровими культурами; WSL-збірка створила `build/ReleaseWithInstall/kefir-hub.nro`.
- [ ] Виправити геометрію списку в "Керуванні модулями" (пункт 14.3; разом з id 44) <!-- id: 46 -->
    - **Проблема з ручного тесту:** список виглядає зміщеним униз; рамка фокусу обрізається зліва, справа і трохи зверху на першому елементі. Через це меню виглядає ніби вкладене в тісне вікно.
    - **Причина в коді:** `uninstaller_menu.cpp` створює `List` з початком `GetY() + 79`, але scissor-контейнер починається на `GetY() + 78`; `drawRectOutline(..., 4.f, v)` виходить за межі прямокутника елемента і відтинається scissor по краях.
    - **Кроки:**
        1. Винести геометрію списку, області обрізання та товщину outline у спільні константи, щоб вони не розходилися на один піксель.
        2. Дати рамці фокусу безпечний внутрішній відступ: або малювати її всередині рядка, або розширити область `nvgScissor` на товщину рамки без виходу за екран. Не використовувати випадкові магічні зсуви.
        3. Підняти початок списку настільки, щоб перший рядок візуально стояв одразу під легендою, але не перекривав її; після зміни описів у id 44 перевірити реальну висоту рядка й видиму кількість модулів.
        4. Перевірити фокус для першого, середнього й останнього елементів, а також скрол після списку, довшого за екран; рамка ніде не обрізається і не перекриває текст.
    - **Перевірка:** перша рамка повністю видима по всьому периметру, список не виглядає втопленим, бокові краї не зрізані; навігація та touch не зміщені.
- [ ] Згрупувати параметри WebDAV в окрему папку у Network settings (пункт 12.1) <!-- id: 47 -->
    - **Проблема:** після id 12 поля `WebDAV URL`, `WebDAV User` і `WebDAV Password` стоять нарівні з FTP, MTP, Nxlink та HDD. Три пов’язані поля перевантажують загальний список Network і не дають користувачу очевидного місця, де налаштовується синхронізація сейвів.
    - **UX-рішення:** у Network лишається один пункт-папка `WebDAV` із коротким описом на кшталт `Configure remote save synchronization.`. Натискання A відкриває правий Sidebar `WebDAV`; у ньому послідовно розміщені URL, User і Password. Порожній URL, як і зараз, вимикає Settings-підключення.
    - **URL UX (додано після ручного тесту):** `webdav://` — внутрішня технічна схема, а не формат, який має знати користувач. У Sidebar поле називається `Server address` і показує дружнє значення без `webdav://`; guide/підказка містить приклад `dav.example.com` або `https://dav.example.com`. Можна ввести голий домен/шлях/порт (`dav.example.com`, `dav.example.com:8443/files`), звичайний `https://...` або наявний `webdav://...`: програма trim-ить текст, перевіряє наявність host і канонізує значення внутрішньо у `webdav://...`, щоб зберегти HTTPS та MKCOL. `http://...` не зберігати через новий Settings UI: показати локалізоване пояснення, що WebDAV sync потребує HTTPS; не мовчки перетворювати HTTP на HTTPS і не залишати конфігурацію, яка зламає автоматичне створення тек. Старі `http://` з `locations.ini` лишаються сумісними та не змінюються.
    - **Контекст:**
        - Поточні три `SettingsItem` лежать прямо в категорії Network у `sphaira/source/ui/menus/settings_menu.cpp` (~2369–2393); вони використовують готові `App::Get/SetWebdavUrl/User/Pass`.
        - Сайдбари вже застосовуються для вкладених налаштувань у `save_menu.cpp:708–781`: `std::make_unique<Sidebar>(..., Sidebar::Side::RIGHT)` + `App::Push` через `ON_SCOPE_EXIT`.
        - `SidebarEntryTextBase` показує значення праворуч і дозволяє задати власний callback (`sphaira/include/ui/sidebar.hpp:161–185`); це підходить для URL/User. Для Password потрібен окремий callback з порожнім initial, `App::SetWebdavPass()` і фіксованою маскою `********`, щоб секрет не показувався.
    - **Кроки:**
        1. У `settings_menu.cpp` винести побудову сайдбара в локальний helper `DisplayWebdavOptions()` або еквівалентний callback; прибрати три прямі `SettingsItem` із Network.
        2. Додати один `SettingsItemKind::Folder` `WebDAV`, який відкриває Sidebar справа. Замість технічного `WebDAV URL` показувати `Server address`: для UI відформатувати наявне канонічне значення без `webdav://`, а порожньому вводу дати guide і/або початковий шаблон `https://` з прикладом домену. Після успішного `swkbd::ShowText` валідовувати host, канонізувати голий домен/`https://`/`webdav://` та лише тоді викликати `App::SetWebdavUrl()`; User так само одразу зберігати й оновлювати entry.
        3. Password відображати тільки як порожній рядок або фіксовані `********`; не передавати старий пароль як initial у клавіатуру. Успішний ввід зберігати через `App::SetWebdavPass()` і оновлювати лише маску.
        4. Для `http://` показати локалізований OptionBox/помилку з вимогою HTTPS і не змінювати попереднє коректне значення. Прямі старі `http://` location entries не фільтрувати й не переписувати: це тільки дружній і безпечний шлях нового Settings UI.
        5. Зберегти чинну нормалізацію URL (`trim`, `https://`/URL без схеми → `webdav://`) та сумісність ручного й автоматичного sync — змінюється тільки навігація UI, не модель даних чи `GetWebdavLocations()`.
        6. Додати en+uk ключі для назви папки, опису, прикладу адреси та помилки HTTP; підняти версію в `sphaira/CMakeLists.txt`, доповнити `walkthrough.md`.
    - **Перевірка:** у Network немає трьох окремих рядків WebDAV; A на `WebDAV` відкриває Sidebar з Server address/User/Password; `dav.example.com`, `https://dav.example.com/` і `webdav://dav.example.com` зберігаються як один канонічний HTTPS WebDAV endpoint, а в UI показуються без технічного префіксу; `http://dav.example.com` показує пояснення й не псує попереднє значення; після закриття і повторного відкриття Sidebar значення зберігаються, пароль лишається замаскованим; ручний sync та auto-sync після зміни налаштувань працюють без регресії.
- [ ] Виправити сенсорну навігацію головного меню Settings (пункт 24) <!-- id: 48 -->
    - **Симптом із ручного тесту:** у Tools сенсор працює коректно, але в головному Settings натискання може виділити не той пункт, перескочити в іншу колонку або взагалі не спрацювати. Це стосується саме двоколонкового меню категорій і параметрів, а не вкладених списків Settings.
    - **Підтверджена причина в коді:** у `settings_menu.cpp` обидва `List` отримують як `pos` загальний `m_pos` меню (`30,87,1190,559`), хоча малюються в різних прямокутниках: категорії `76,138,300,56`, параметри `420,132,780,66`. `Update()` спочатку перевіряє `m_category_list->GetPos()`, тому ця широка зона завжди перехоплює натискання правої колонки; `else if` для параметрів практично недосяжний. Потім touch-обробник категорій шукає рядок уже за фактичною геометрією й не знаходить його — звідси пропущені натискання. Це відрізняється від одноколонкового Tools, де `List` має одну узгоджену з малюванням область.
    - **Кроки:**
        1. Задати кожному `List` власний viewport, який точно охоплює його видимі рядки та скрол-область: для категорій — ліва колонка, для параметрів — права. Не передавати їм загальний `MenuBase::m_pos`.
        2. Перебудувати визначення `FocusPane` так, щоб один tap оброблявся лише списком, під геометрією якого він лежить; торкання категорії вибирає категорію, торкання параметра — параметр. Клік по вже вибраному параметру відкриває його так само, як A; клік по вже вибраній категорії не має випадково активувати параметр.
        3. Перевірити узгодженість `GetPos()`, координат рядка `m_v`, padding та меж після скролу. Не змінювати загальну логіку `List::OnUpdate`, якщо достатньо правильних viewport Settings: це зменшує ризик регресії в Tools та інших меню.
        4. Протестувати ручний touch: перший/середній/останній видимий рядок обох колонок, зміну категорії, параметри після зміни категорії, прокрутку довшого списку, швидкі послідовні торкання та відкриття Sidebar/OptionBox. Окремо перевірити, що Tools лишився без регресії.
        5. Підняти версію в `sphaira/CMakeLists.txt`, додати запис у `walkthrough.md`; нові i18n-рядки не потрібні, якщо діагностичні повідомлення не додаються. Збірку виконує Агент 1 у WSL.
    - **Перевірка:** натискання в лівій колонці завжди змінює лише категорію, у правій — лише відповідний параметр; жодна зона не "з’їдає" натискання іншої; перший, середній та останній рядки збігаються з візуальною рамкою фокусу до і після скролу; усі звичайні сенсорні дії у Tools працюють як раніше.
- [x] Зробити WebDAV-синхронізацію бекапів зрозумілою в контекстному меню сейву (пункт 25) — реалізовано у v0.13.154, розміщення виправлено у v0.13.155 <!-- id: 49 -->
    - **Проблема з ручного тесту:** пункт `Синхронізувати з хмарою` стоїть поруч із `Backup` і `Restore` у `Save Action`. З назви неясно, що саме синхронізується, у якому напрямку, чи охоплює це всі старі бекапи, як після цього відновити дані й де перевірити файли на сервері. Через це він виглядає як третя, незрозуміла операція над сейвом, а не як параметр зберігання резервних копій.
    - **Поточна фактична поведінка:** `SyncSavesRemoteWithLocation()` у `save_menu.cpp:1930–2030` працює лише для поточно вибраних ігор/типів сейвів (`GetSelectedEntries()`). Вона порівнює назви ZIP-архівів у локальній теці й на WebDAV: відсутні на сервері ZIP завантажує, відсутні на SD ZIP завантажує назад. Вона не передає «живий» save напряму та не охоплює всі бекапи на SD автоматично. Однакове ім’я в обох місцях вважається вже синхронізованим без порівняння вмісту. Auto-sync після Backup ще вужчий: він вивантажує тільки щойно створений/найновіший ZIP кожної забекапленої вибраної гри.
    - **Фізичний шлях на перевірочному сервері:** для поточної схеми це `/srv/sphaira-webdav/sphaira-saves/<Save type>/<Game name або Title ID>/<timestamp>.zip`; для звичайного Account save, наприклад, `/srv/sphaira-webdav/sphaira-saves/Save/<назва гри>/<дата>.zip`. Це є серверним відображенням SD-шляху `/dumps/<Save type>/<Game name>/...zip`. Після id 38 структура зміниться на `sphaira-saves/<Гра>/<Тип>/...zip`, тому інструкцію слід формулювати через команду пошуку, а не прив’язувати до одного дерева.
    - **Уточнене UX-рішення:** `Sync with remote` лишається в контекстному `Save Action`, бо це дія саме над вибраним зараз сейвом. Кнопку/перемикач постійної auto-sync поведінки також перенести з `Save Options → Advanced` у це контекстне меню, поруч із ручною синхронізацією, щоб обидві дії були в одному місці. Перед стартом показувати підтверджувальний OptionBox/поп-ап із кнопками `Sync` / `Cancel` і зрозумілою інструкцією. Текст має прямо сказати: «Синхронізуються ZIP-бекапи вибраних сейвів у два боки. Усі архіви, яких бракує на іншому боці, будуть скопійовані: нові локальні — у хмару, нові хмарні — на SD. Файли з однаковим ім’ям не перезаписуються та не порівнюються за датою чи вмістом. Після завантаження з хмари виберіть Restore». Це пояснює роль часу чесно: нові бекапи мають timestamp у назві, але алгоритм не вибирає один «найсвіжіший», а переносить усі відсутні архіви.
    - **Кроки:**
        1. Залишити `SyncSavesRemote()` у `PromptSaveTypeOptions()`/`Save Action`, але замість негайного запуску показати новий підтверджувальний OptionBox. Лише позитивна дія поп-апу запускає наявний вибір WebDAV location та `SyncSavesRemoteWithLocation()`; Cancel не змінює файли.
        2. Додати en+uk i18n для заголовка, кнопок і повного пояснення двобічної синхронізації ZIP: межа «лише вибрані зараз сейви», усі відсутні архіви, локальний → WebDAV, WebDAV → SD, відсутність перезапису однакових імен та шлях `Sync → Restore`. Не називати дію «завантажити найсвіжіший», якщо алгоритм не порівнює timestamps.
        3. Перенести `Auto-sync saves after backup` у контекстне меню Save Action і чітко описати його область: він вивантажує щойно створений найновіший ZIP після Backup, а не синхронізує всю бібліотеку. Ручна sync у тому самому меню розглядає всі відсутні ZIP вибраних сейвів. Не міняти семантику синхронізації без окремого рішення про конфлікти.
        4. Додати у `walkthrough.md` розділ для користувача й рев’юера: як перевірити HTTPS WebDAV (`PROPFIND`), як переглянути архіви на Cooler (`sudo find /srv/sphaira-webdav/sphaira-saves -type f -name '*.zip'`), як прочитати поп-ап, вручну завантажити відсутні бекапи та потім виконати Restore. Явно зазначити, що однакові імена ZIP не звіряються за вмістом, а віддалене видалення не видаляє локальний файл.
        5. Ручно протестувати: один локальний ZIP → upload із першим progress bar; один ZIP лише на сервері → download на SD із другим progress bar; змішані набори → послідовно обидва напрямки; однаковий файл не дублюється; Restore бачить завантажений ZIP. Окремо перевірити перемикач auto-sync у контекстному меню, що Backup/Restore не змінилися, а авто-sync вивантажує тільки новий бекап.
        6. Підняти версію в `sphaira/CMakeLists.txt`, оновити `walkthrough.md`; збірку виконує Агент 1 у WSL.
    - **Прогрес:** синхронізація виконується двома послідовними фазами з окремими progress bar: `Local → WebDAV` показує всі upload-и, потім `WebDAV → SD` показує всі download-и. Кожна фаза має власний поточний/загальний лічильник; друга не починається, доки перша не завершена.
    - **Перевірка:** користувач із поп-апу перед стартом розуміє, що це синхронізація ZIP-бекапів вибраних сейвів у два боки, що переносяться всі відсутні архіви, а не один «найсвіжіший», і що дублікати за іменем не перезаписуються; бачить окремий прогрес upload-фази й download-фази; може знайти архіви на сервері, виконати `Sync → Restore` і відновити завантажений з WebDAV бекап; Cancel не запускає мережеву операцію, а auto-sync у контекстному меню має окремий точний опис.
    - **Реалізовано у v0.13.154:** перед sync показується підтверджувальний OptionBox із повним поясненням двобічної синхронізації ZIP і кнопками `Sync`/`Cancel`; лише підтвердження веде до вибору WebDAV-локації. Тумблер auto-sync перенесено з `Save Options → Advanced` у `Save Action` як пункт `Auto-sync after backup: On/Off` із власним поясненням області дії (лише щойно створений ZIP після Backup). `SyncSavesRemoteWithLocation()` будує повний план до передач і виконує дві послідовні фази `Local → WebDAV (i/N)` та `WebDAV → SD (i/M)` з per-file прогресом (`curl::OnProgress`) і перевіркою скасування між файлами. У walkthrough додано інструкції PROPFIND/пошуку архівів на сервері та сценарій `Sync → Restore`.
    - **Виправлено у v0.13.155 (ручне рев'ю користувача):** розміщення в `Save Action` відхилено — `Save Action` знову має рівно дві дії (Backup/Restore). `Sync with remote` і `Auto-sync after backup` перенесено в `Backup Options` (секція `SYNC`, тільки для Backup) з власними tooltip-описами; проміжний OptionBox для перемикання auto-sync прибрано (прямий тумблер, як і решта Advanced-опцій). Прогрес синхронізації переписано з per-file (`curl::OnProgress` на кожен файл) на агрегований по напрямку: окремий бар для `Local → WebDAV` і окремий для `WebDAV → SD`, де лічильник — кількість завершених файлів фази, а не байти поточного файлу; бар ніколи не скидається в середині фази.
    - **Виправлено у v0.13.157 (ручний тест):** розміщення в `Backup Options` теж відхилено — `Sync with remote`/`Auto-sync after backup` перенесено в `Save Options` (кнопка `+`/START зі списку сейвів), секція `SYNC` між `Data Types` і `Advanced`; `Backup Options` більше не містить нічого про sync. Виявлено регресію v0.13.155: рахунок-по-файлах без `curl::OnProgress` означав, що для типової синхронізації (1–2 файли) бар стояв нерухомо весь час передачі й лише стрибав у момент завершення файлу — на пристрої це виглядало як повна відсутність прогрес-бару. Виправлено на живий агрегований прогрес: кожній фазі виділено `PROGRESS_SCALE` (1 000 000) умовних одиниць на файл, `curl::OnProgress` поточного файлу мапить реальний `dlnow/dltotal` (або `ulnow/ultotal`) у частку цього бюджету й додає до вже завершених файлів — бар одночасно і рухається протягом кожної передачі, і ніколи не скидається на 0% між файлами.
    - **Уточнено у v0.13.159 (повторний ручний тест):** перенесення `Auto-sync after backup` у `Save Options` у v0.13.157 було помилковим — це властивість саме дії Backup (одразу вирішити, чи вивантажити щойно створений бекап у хмару), тому її місце поруч із самою дією. Повернуто в `Backup Options` (тільки Backup, не Restore), одразу після `Location`. У `Save Options` лишається тільки `Sync with remote`.
    - **Виправлено у v0.13.160 (ручний тест):** авто-синк після Backup (`Menu::BackupSaves()`) досі не мав жодного прогрес-бару — на відміну від `SyncSavesRemoteWithLocation()`, цей код ніколи не редагувався попередніми фіксами й ніколи не мав `curl::OnProgress`. Винесено спільний хелпер `MakeAggregateProgressCb()`/`SYNC_PROGRESS_SCALE` (та сама логіка живого, не-скидається-між-файлами прогресу) і застосовано його до авто-синку так само, як і до ручного sync.
    - **Виправлено у v0.13.162 (ручний тест):** бар авто-синку стрибав на межах файлів. Дві причини: (а) `NewTransfer()` викликався на кожен файл і скидав offset/size у 0 — тепер один `NewTransfer("Local → WebDAV")` перед циклом, ім'я файлу через `SetActionName()`; (б) curl-колбек після тіла upload продовжує кликатися для фази відповіді сервера з обнуленими ul- і крихітними dl-лічильниками — частка файлу провалювалась до нуля; тепер напрямок (`is_upload`) передається явно, протилежні лічильники ігноруються, а показане значення монотонне (`max`), тож бар не може рухатись назад. Також на прохання користувача повністю прибрано підтверджувальне віконце перед `Sync with remote` (дублювало tooltip): повний текст пояснення об'єднано в tooltip пункту в `Save Options`, ключі `Start sync?`/`Sync` видалено з en/uk.
- [x] Покращити вибір і відображення папки для сейв-бекапів (пункт 27) — реалізовано у v0.13.155, алгоритм рядка Location та мітку SD доопрацьовано у v0.13.156, залишковий баг мітки виправлено у v0.13.157 <!-- id: 52 -->
    - **Проблема з ручного тесту:** у `Backup Options`/`Restore Options` під стандартною `/dumps` не зберігаються останні папки, які користувач обирав сам; частина підписів (`Location`, `Choose Folder...`, `Backup Options`, `microSD card`, `at`) не перекладена або технічно сформульована. На довгому шляху назва пункту перекривається значенням папки.
    - **Контекст:** побудова списку локацій і `MakeLocationLabel()` — `sphaira/source/ui/menus/save_menu.cpp` (актуально ~1432–1558); вибір довільної папки — `filepicker::Menu` у `sphaira/source/ui/menus/file_picker.cpp` та `sphaira/include/ui/menus/file_picker.hpp`; стандартний пункт використовує `/dumps`, а SD-джерело файлового picker-а має назву `microSD card`.
    - **Рішення:**
        1. Під стандартною локацією `/dumps` показувати до п’яти останніх унікальних папок, які користувач підтвердив через `Choose Folder...`; порядок — від найновішої до найстарішої. Не дублювати `/dumps`, поточну папку та однакові шляхи; після вибору існуючий backup/restore сценарій не змінювати.
        2. Зберігати історію між перезапусками у чинному конфігураційному механізмі застосунку; обмежити її п’ятьма записами, акуратно обробити відсутні/переміщені папки та не ламати старі конфіги.
        3. Додати всі видимі UI-рядки через `_i18n` і ключі до `assets/romfs/i18n/en.json` та `assets/romfs/i18n/uk.json`. Замість `microSD card` у користувацьких підписах використовувати локалізований короткий формат `SD:`; технічні імена mount/path не змінювати.
        4. Переробити відображення довгого шляху в рядку Location: якщо шлях може перекрити заголовок або займає понад половину відведеної ширини, показувати його повністю з переносом на наступний рядок; якщо він не вміщується й там — використовувати горизонтальне прокручування. Для коротших шляхів також залишити прокручування, щоб повний текст був доступний без обрізання.
        5. Узгодити висоту Sidebar/рядка, фокус, touch і scissor-області з дворядковим значенням; назва `Location` не повинна перекриватися шляхом. Перевірити Backup і Restore, SD та stdio-папки.
    - **Перевірка:** після вибору шести різних папок список показує `/dumps` і лише п’ять останніх; повторний вибір переміщує папку на початок без дубліката; після перезапуску історія зберігається; у чистій конфігурації меню працює як раніше; всі нові/наявні підписи мають en/uk переклад; `SD:` використовується замість `microSD card`; короткий шлях не перекриває заголовок, довгий переноситься або прокручується, а вибір і підтвердження не зміщені.
    - **Реалізовано у v0.13.155:** `Menu::AddRecentBackupDir()`/`GetRecentBackupDirs()` зберігають до 5 останніх унікальних папок у `option::OptionString[5]` (`[saves] recent_backup_dir_0..4`) з дедуплікацією та move-to-front; список показується одразу під `/dumps`, перед stdio-накопичувачами, пропускаючи збіги з `/dumps` чи вже показаним stdio-шляхом.
    - **Доопрацьовано у v0.13.156 (уточнення користувача):** попередній горизонтальний скрол відхилено на користь точного алгоритму кроку 4/5. `SidebarEntryBase::DrawEntry()` тепер: (1) якщо ширина лейбла > 50% робочої ширини рядка — значення переноситься на другий рядок під лейблом; (2) інакше, якщо ширина значення > 75% — так само перенос на другий рядок; (3) інакше лейбл займає лише свою мінімальну потрібну ширину, а решта (мінус відступ) віддається значенню, вирівняному по правому краю; (4) у всіх випадках, якщо тексту все одно не вистачає виділеного простору — скрол при фокусі. Обидва рядки 2-рядкового режиму вміщуються в наявну висоту рядка Sidebar (70px) без зміни геометрії/scissor `List`, тож регресії в інших Sidebar (WebDAV, Module Manager тощо) немає. Мітка SD-локацій — `MakeSdLocationLabel()` повертає `"sd://" + шлях` (напр. `sd:///dumps`) замість `"SD: /dumps"`, у стилі наявного `webdav://`; ключ i18n `"SD"` видалено як зайвий.
    - **Виправлено у v0.13.157 (ручний тест):** мітка все ще показувала `"microSD card: <шлях>"` одразу після вибору папки через `Choose Folder...` — `MakeSdLocationLabel()` застосували до стандартного `/dumps` і до записів історії, але пропустили сам filepicker-callback, де лейбл будувався напряму з `fs_entry.name` (технічна назва SD-джерела). Виправлено: одразу після вибору SD-папки лейбл теж `sd://<шлях>`; stdio (USB/HDD) без змін.
    - **Виправлено у v0.13.158 (ручний тест):** `sd://` + абсолютний шлях (що вже починається з `/`) давав потрійний слеш `sd:///dumps`. Провідний `/` шляху тепер прибирається перед конкатенацією — результат `sd://dumps` (два слеші, як у `webdav://`).
    - **Уточнено у v0.13.161 (ручний тест):** правило «лейбл > 50% → перенос» v0.13.156 було надто агресивним: `Auto-sync after backup: On` розносило на два рядки, хоча `On` вміщується в залишку. Нове правило: перенос — лише коли ОБИДВІ частини окремо > 75% ширини рядка; інакше один рядок — коротша частина отримує природну ширину (кеп 75%), довша забирає залишок і скролиться при фокусі, якщо не вміщується.
    - **Виправлено у v0.13.163 (статичний аудит):** (1) крок 3 був неповний — до `uk.json` додано 16 відсутніх ключів блоку Location/Backup (`Backup Options`, `Restore Options`, `Start Backup`, `Start Restore`, `LOCATION`, `Location`, `Choose Folder...`, `ACCOUNTS`, `SAVE TYPES`, `Select Sync Location`, `No matching saves found.` + tooltip'и), які показувались англійською; (2) крок 2 (зниклі/переміщені папки) не був реалізований — `GetRecentBackupDirs()` тепер фільтрує через `RecentBackupDirExists()` (SD: `fs::DirExists(path)`; stdio: `fs::DirExists(mount+path)`, відключений носій відсіюється), історія в конфізі не видаляється; (3) `AddRecentBackupDir` дедуплікує за `MakeLocationKey` (ім'я ігнорується) — узгоджено з walkthrough v0.13.155; (4) filepicker-callback не дублює рядок у сесії (паралельний `location_keys` в `ActionState`); (5) `SidebarEntryBase::OnFocusLost` скидає й `m_scolling_value`. **Sync-логіка:** текст швидкості під час sync (синтетичні одиниці як `MiB/s`) приховано через `ProgressBox::SetHideSpeed(true)` зі збереженням коректного ETA; прибрано осиротілий ключ `Downloading: ` з en/uk. Збірку виконує Агент 1.
- [x] Додати вибір конкретного бекапу при відновленні + віддалені бекапи з хмарною іконкою — реалізовано у v0.13.164 <!-- id: 53 -->
    - **Запит користувача:** відновлення завжди тихо брало найновіший бекап; треба показувати список бекапів із датами й дозволяти обрати не-останній. Також: галка «показувати віддалені» → перед показом робити синхронізацію (лише завантаження відсутніх), у списку локальні й віддалені разом, віддалені — з іконкою хмари зліва (не зміщати текст рядка). Плюс прибрати «галочку» з пунктів Backup/Restore у «Save Action» (це навігація, не вибір значення) — замінити на шеврон.
    - **Формат (уточнено):** ігрові сейви вже пишуться тільки у DBI-форматі (читаються DBI). Sphaira-формат лишається лише для системних сейвів — DBI-ім'я будується навколо Title ID, якого System/SystemBcat не мають, тож DBI їх не бекапить. Другий формат ще читається для відновлення старих бекапів. Рішення користувача (2026-07-11): лишити як є.
    - **Рішення (реалізовано у v0.13.164):**
        1. `FindLatestBackupPath` → `CollectBackups()` (усі кандидати, найновіший перший, дедуп за шляхом); `FindLatestBackupPath` — тонка обгортка.
        2. `StartRestore()`: мультивибір → стара поведінка (найновіша кожного); один сейв → `ShowRestorePicker()` (0 → «не знайдено», 1 → одразу, >1 → `PopupList` дат) → `RestoreSavesPicked()`.
        3. Галка `Include remote backups` (`m_restore_include_remote`, persistent) у `Restore Options`; при ввімкненні для одного сейву — `DownloadRemoteBackupsForEntry()` (download-only) перед пикером; завантажені позначаються хмарою.
        4. `PopupList::SetMenuStyle` (шеврон замість tick) для «Save Action»; `PopupList::SetRemoteMarkers` (хмара у лівому жолобі); векторні `DrawCloudIcon`/`DrawChevron`.
        5. i18n: `Include remote backups` + tooltip, `Select backup`, `No backups found for selected saves.` (en/uk).
    - **Перевірка (Агент 1):** один сейв із кількома бекапами → список дат, вибір старішого відновлює саме його; один бекап → без пикера; мультивибір → без пикера (найновіша кожного); `Include remote backups` тягне відсутні з WebDAV і показує хмару; `Save Action` показує шеврон. Збірку виконує Агент 1.
    - **Виправлено у v0.13.165 (багатоагентне рев'ю власного диффу):** 8 підтверджених знахідок. Суттєві: (1) бекапи з нерозпізнаваним іменем (ts==0) стали невідновлюваними — `CollectBackups` відкидала їх, тепер знову приймає (сортуються в кінець, у пикері показані під назвою файлу); (2) `CollectBackups` блокувала UI-потік у `ShowRestorePicker` (сканування тек + відкриття кожного dbi-zip) — тепер виконується у `ProgressBox`-воркері, результат передається у новий `ShowRestorePickerPopup()`; (3) втрачено детермінований tie-break при однакових timestamp (dbi мав пріоритет) — `std::ranges::sort` за самим `ts` нестабільний, повернуто `source` як другий ключ. UX/i18n: (4) OptionBox «No WebDAV…» одразу перекривався пикером — прибрано, тихий fallback; (5) тип сейву в рядку пикера був неперекладений і однаковий для всіх кандидатів — суфікс прибрано. Дублювання: (6) `MakeFsForLocation()` замінив 4 копії вибору fs (дві мали небезпечний bare-else); (7) `DownloadOneBackupFile()` замінив ~30 рядків дублювання download-фази між `DownloadRemoteBackupsForEntry` і `SyncSavesRemoteWithLocation`; (8) прибрано зайвий паралельний вектор `paths` у пикері.
- [x] Виправити auto-sync після Backup: сканувати локацію, у яку зроблено бекап, а не завжди SD (Крок S1 з implementation_plan.md) — реалізовано у v0.13.201 <!-- id: 63 -->
    - **Проблема (аудит):** завершальна лямбда `Menu::BackupSaves(entries, location, backup_root)` не захоплювала `location`, а автосинк жорстко створював `fs::FsNativeSd` — бекап у stdio-локацію (USB/HDD, Recent-папка) призводив до мовчазного пропуску або, гірше, до вивантаження старішого SD-бекапу з повідомленням «Auto-sync successfull!».
    - **Реалізовано у v0.13.201:** `location` захоплюється обома лямбдами; `fs::FsNativeSd` замінено на `MakeFsForLocation(location)`; `FindLatestBackupPath` викликається з `fs.get()` — шляхи будуються від `fs->Root()`, інших змін не потрібно.
- [x] Стійкість Sync with remote: не обривати весь sync на першому невдалому файлі (Крок S4 з implementation_plan.md) — реалізовано у v0.13.202 <!-- id: 64 -->
    - **Реалізовано у v0.13.202:** невдалі upload/download збираються у локальний `failed` і цикл триває; лічильник прогресу інкрементується у будь-якому разі; скасування користувачем далі перериває все одразу (`ShouldExitResult()` на початку ітерації + перевірка `ShouldExit()` в обох гілках невдачі). Непорожній `failed` → `Result_SaveSyncFailed`, а завершальний колбек показує «Sync finished with N failed transfers. See log for details.» (кількість — через захоплений `std::shared_ptr<size_t>`); інші помилки зберігають старий `App::PushErrorBox`. `DownloadOneBackupFile` (temp+rename) і `DownloadRemoteBackupsForEntry` не змінені. i18n en+uk.
- [x] Константа `DEFAULT_BACKUP_ROOT` та чесні тексти tooltip'ів `Sync with remote` і `Location` (Кроки S2+S3 з implementation_plan.md) — реалізовано у v0.13.203 <!-- id: 65 -->
    - **Реалізовано у v0.13.203:** `inline constexpr const char* DEFAULT_BACKUP_ROOT = "/dumps";` у `save_paths.hpp` замінив жорсткі літерали у `BackupSaves`/`RestoreSaves`/`SyncSavesRemoteWithLocation` (`save_menu_ops.cpp`) та `default_backup_root` (`save_menu.cpp`); літерал лишився тільки в default-аргументі `BackupSaveInternal` у `save_menu.hpp` (циклічне включення, задокументовано коментарем). Tooltip `Sync with remote` тепер явно описує SD-only-межу (тека `/dumps` + DBI-тека `/switch/DBI/saves`; інші папки/носії не охоплюються); tooltip `Location` пояснює, що ігрові сейви завжди пишуться у DBI-форматі до `/switch/DBI/saves` на обраному носії, а вибрана тека — для системних бекапів і пошуку старіших бекапів при Restore. Старі ключі i18n замінено/видалено синхронно в en/uk; поведінка без змін.
- [x] Переробити DBI Backend у чергу інсталяції з перевіркою, вибором і журналом (пункт 26) — реалізовано у v0.13.153 <!-- id: 50 -->
    - **Проблема:** після отримання DBI-списку `dbi_menu.cpp` одразу створює `ProgressBox` і в циклі викликає `yati::InstallFromSource()` для кожного імені. Користувач не бачить, що саме надіслав ПК, не може виключити окремі ігри, перевірити місце чи скасувати операцію до першого запису в content storage. Поточний detached popup призначений для фонових MTP/web-передач і не є повноцінним екраном інсталяції з журналом.
    - **Підтверджений технічний контекст:** DBI0 уже віддає повний список імен у `DbiUsb::WaitForConnection()` (`yati/source/usb_dbi.cpp:23–60`); дані кожного файлу читаються на вимогу random-access командами `FileRange` тільки пізніше. Отже після списку можна залишати USB-сесію відкритою, прочитати заголовки/контейнер у режимі аналізу й не починати запис. Зараз `yati::InstallFromSource()` одразу парсить контейнер і встановлює; `InstallFromCollections()` для NSZ/XCZ застосовує лише приблизний коефіцієнт `×1.6` при виборі носія (`yati.cpp:1582–1627`), тому його не можна показувати користувачу як точний «розпакований розмір».
    - **UX-рішення:** після надходження списку відкривати повноекранний `Install queue`, а не стартувати інсталяцію. У ньому: список пакетів з чекбоксами; назва/тип/Title ID за можливості; розмір файлу на ПК; реальний прогнозований обсяг встановлення; сумарний обсяг вибраного; вільне місце SD і системної пам’яті після резерву; вибір `Auto` / `microSD` / `System memory`; кнопки `Install selected` і `Cancel session`. Після підтвердження відкривати повноекранний екран інсталяції з прогресом черги й прокручуваним пальцем журналом подій, а не MTP/web ProgressBox. Помилка одного пакета лишається у журналі з назвою та Result-кодом; користувач може перейти до наступного або завершити чергу за явно визначеним правилом.
    - **Кроки:**
        1. Розділити DBI-сесію на стани `WaitingForList → Analysing → ReviewQueue → Installing → Summary/Cancelled`. Після `WaitForConnection()` передавати список на головний потік; не викликати `InstallFromSource()` до натискання `Install selected`. При B/Cancel коректно послати DBI `Exit`/скасування, зупинити воркер і лише тоді повернути MTP, якщо він був увімкнений.
        2. Винести з Yati чистий preflight API, наприклад `AnalyzeSource(source, path) → InstallAnalysis`: парсинг контейнера без `ncm`/`ns` запису, title metadata та перелік content. Для NSP/XCI рахувати суму content, що буде записаний. Для NSZ/XCZ визначати точний розпакований розмір із NCZ/контейнерних метаданих; якщо конкретний формат або файл не дає достовірного розміру до повного читання — показувати `Estimate` з причиною, а не маскувати `×1.6` як факт. Помилки аналізу показувати у рядку пакета й не дозволяти вибрати його без явної повторної спроби.
        3. Створити окремий `DbiInstallQueueMenu`/dashboard із `List`: A вмикає/вимикає пакет, X — select all/none, плюс окремий екран або панель деталей. Показати підсумок лише вибраних файлів: кількість, потрібне місце, доступне SD/NAND, резерв і результат перевірки для кожного кандидата. Не покладатися лише на назву файлу: title/тип береться з аналізу, а коли він недоступний — чесно показати filename.
        4. Додати вибір носія на рівні черги: `Auto` використовує чинне правило Yati (обирає носій з урахуванням reserve), `microSD` та `System memory` передають явний `ConfigOverride::sd_card_install`. Перед кнопкою Install перевіряти сумарний план за цільовим носієм і попереджати, якщо місця недостатньо; не розподіляти один пакет між носіями. Для Auto показати запропонований носій кожного пакета і дозволити перед підтвердженням замінити його на загальний явний вибір.
        5. Замість `ProgressBox` DBI-інсталяції реалізувати повноекранний `InstallLogMenu`: поточний пакет, загальний і пакетний прогрес, швидкість/етап Yati, підсумок успіхів/помилок та кільцевий буфер структурованих рядків журналу. `log_write` не читати як UI-транспорт: додати безпечний callback/event від DBI/Yati в головний потік. Список логу має підтримувати touch-scroll через `List` або дороблений `ScrollableText` (його touch Update зараз вимкнений), кнопки для паузи/скасування лише там, де протокол і Yati це реально підтримують.
        6. Визначити політику черги до коду: за замовченням після помилки одного файлу продовжувати наступні вибрані, позначати failure у Summary; Cancel зупиняє поточний/наступні без приховування вже встановлених пакетів. Не проголошувати rollback: інсталяція на Switch не є транзакцією для кількох пакетів.
        7. Зберегти сумісність з DBI-клієнтом: під час ReviewQueue джерело має відповідати на потрібні `FileRange` запити для аналізу; перевірити timeout і коректне завершення сесії, якщо користувач довго читає чергу. Окремо не змішувати цей екран із MTP root-drop та Web Server: вони можуть лишитися на своєму transfer UI.
        8. Додати en+uk i18n, підняти версію у `sphaira/CMakeLists.txt`, описати новий протокол UX і сценарії помилок у `walkthrough.md`; збірку виконує Агент 1 у WSL.
    - **Перевірка:** DBI-клієнт надсилає 2–3 NSP/NSZ/XCZ, а консоль не записує нічого до `Install selected`; користувач бачить і вимикає окремий пакет, бачить розмір і позначку `Exact` або `Estimate`, вільне місце й обраний носій; Auto вибирає носій за наявною політикою, явний SD/NAND її перевизначає; інсталяція йде у повноекранному журналі, який прокручується пальцем; помилка одного пакета не зникає з історії, Cancel завершує DBI-сесію чисто, MTP повертається, а старі MTP/web install-шляхи не регресують.
    - **Реалізовано:** DBI-сесію розділено на очікування USB-підключення, очікування DBI-списку, read-only аналіз, повноекранний перегляд черги, встановлення та підсумок/скасування. До `UsbState_Configured` UI просить підключити кабель і перевірити визначення консолі; після конфігурації USB прямо каже, що можна вибрати пакети й натиснути Start у DBI Backend. До `Install selected` Yati лише парсить контейнер через новий `AnalyzeSource()` без ініціалізації `ncm/ns` і без створення placeholders. Черга використовує звичне керування файлового менеджера: X вибирає поточний пакет, Y інвертує вибір, A запускає наступну дію; checkbox малюється NanoVG/glyph, а не ASCII. R3 циклічно задає `Auto/microSD/System memory` окремо для поточного пакета; A/підтвердження low-space атомарно фіксує immutable snapshot selection+target усіх пакетів, тому перевірений план не може змінитися до install. Exact/Estimate, доступне місце й reserve лишаються видимими, арифметика перевіряється на overflow. Install-екран показує швидкість MiB/s і поточний content/етап; журнал має окремий компактний touch-scroll список, а нові рядки не збивають ручний scroll, якщо користувач відійшов від кінця. Recoverable package-помилки не зупиняють наступні пакети, але cancel або USB/session error безпечно завершують решту черги, не приховуючи попередні успіхи. Cancel завершує/скасовує DBI USB-сесію перед відновленням MTP. Залежність Yati від конкретного `ProgressBox` замінено вузьким `InstallProgress`, при цьому MTP/Web/File шляхи й далі використовують старий `ProgressBox`. Runtime follow-up v0.13.151 виправляє зависання `Analysing` з DBI Backend Qt одним USB payload; v0.13.152 додає окремі стани USB/list; v0.13.153 узгоджує queue/log UX з іншими меню.
- [ ] Переробити екран прогресу інсталяції черги (`State::Installing` у `dbi_menu.cpp`): графік R/W швидкості замість тексту, друга (загальна) полоска прогресу (продовження id 50, за скаргою з ручного тесту) <!-- id: 54 -->
    - **Проблема з ручного тесту:** поточний `Draw()` для `State::Installing` (`sphaira/source/ui/menus/dbi_menu.cpp:272–298`) віддає майже весь верх екрана під два рядки дрібного тексту (`Package X/Y`, `Installed/Failed`, `Speed: N MiB/s` — усе в один рядок 18px) і одну тонку полоску прогресу (`GetY()+67`, висота 12px) під ними. Під полоскою до списку журналу лишається великий порожній прямокутник. Швидкість — найважливіша метрика під час install — загублена серед лічильників у суцільному рядку тексту й ніяк не виділена кольором чи розміром. Немає загального прогресу черги (скільки з усього байтажу вибраних пакетів уже встановлено), лише прогрес поточного пакета.
    - **Підтверджений технічний контекст:**
        - Read і write в Yati вже виконуються в окремих потоках через `RingBuf` (`sphaira/source/yati/yati.cpp:212–273,294–295`), а не синхронним copy-loop, тому read-throughput (дані, що приходять із джерела — USB DBI, MTP тощо) і write-throughput (запис у content storage) — це два реально різні виміри, а не один і той самий showable number.
        - `ThreadData` уже рахує write окремо: `write_offset`/`write_size` і геттери `GetWriteOffset()`/`GetWriteSize()` (`yati.cpp:171–177`), які й читає `pbox->UpdateInstallTransfer(...)` (`yati.cpp:1017`). Симетричний `read_offset`/`read_buffer_size` уже існує (`yati.cpp:307,379–384`), але немає публічного геттера й ніхто його назовні не віддає.
        - `InstallProgress` (`sphaira/include/ui/install_progress.hpp`) — єдиний інтерфейс, яким Yati повідомляє прогрес у UI (`UpdateInstallTransfer(offset, size)`), і його вже реалізує вузький `InstallProgress` DBI-черги окремо від старого `ProgressBox` (MTP/Web/File-шляхи), тому розширення інтерфейсу для DBI не зачіпає інші джерела встановлення, якщо додати новий метод, а не змінювати сигнатуру наявного.
        - `dbi_menu.cpp:272–286` вже рахує "write speed" через EMA (`m_progress_speed`, коефіцієнт 0.25/0.75) з дельти `m_progress_offset` кожні 0.5с — цю логіку потрібно узагальнити на два незалежні лічильники (read/write) і на другий, загальний offset/size для всієї черги.
    - **UX-рішення:**
        - Прибрати щільний текстовий рядок лічильників з верхньої шапки; лишити зверху лише один компактний рядок статусу (`Package X/Y`, `Installed`, `Failed`) і, за наявності, назву поточного файлу/етапу.
        - У звільненому просторі шапки намалювати невеликий лінійний графік швидкості за останні ~30–60 семплів (0.5с крок, як зараз): дві лінії — `R` (read, синій) зверху та `W` (write, зелений) знизу або накладені з різним кольором і легендою `R`/`W` збоку. Праворуч або над графіком — поточне числове значення швидкості великим шрифтом того ж кольору для кожної лінії (наприклад "R 42.3 MiB/s" синім, "W 38.1 MiB/s" зеленим), щоб швидкість була найпомітнішим елементом екрана, а не дрібним текстом серед лічильників.
        - Під шапкою лишити дві полоски прогресу одна під одною: перша (як зараз) — прогрес поточного пакета (`m_progress_offset`/`m_progress_size`), друга, нижче — загальний прогрес черги (сума встановлених байтів усіх завершених + поточного пакета відносно сумарного `install_size` вибраних пакетів). Підписати обидві коротким лейблом ліворуч або над ними (`Current`/`Overall`), щоб не плутати.
        - Журнал (`m_log_list`) лишається знизу без змін розміру логіки скролу — лише геометрія (`GetY()` offset) зсувається вниз під нову шапку/графік/другу полоску.
    - **Кроки:**
        1. У `ThreadData` (`yati.cpp`) додати `GetReadOffset()`/`GetReadSize()` симетрично до `GetWriteOffset()`/`GetWriteSize()`; у точці, де зараз викликається `pbox->UpdateInstallTransfer(t_data.GetWriteOffset(), t_data.GetWriteSize())` (`yati.cpp:1017`), додати виклик нового методу для read.
        2. Розширити `InstallProgress` (`install_progress.hpp`) новим віртуальним методом, наприклад `UpdateInstallReadTransfer(s64 offset, s64 size)`, з дефолтною порожньою реалізацією (`virtual void ... {}`), щоб не ламати наявні реалізації `ProgressBox`/MTP/Web, які цей сигнал не показують.
        3. У DBI-чергу (`dbi_menu.hpp`/`.cpp`) додати паралельні до `m_progress_*` поля для read (`m_read_offset`, `m_read_last_offset`, `m_read_speed`) і для загального прогресу черги (`m_queue_bytes_done`, `m_queue_bytes_total` — рахується один раз при фіксації snapshot після `Install selected`, з `install_size` вибраних пакетів; `m_queue_bytes_done` оновлюється на завершення кожного пакета + поточний прогрес).
        4. Додати кільцевий буфер семплів швидкості (наприклад `std::array<float, 64>` або `boost`-style ring, окремо read/write) для графіка; заповнювати в тому ж місці, де зараз рахується EMA (`dbi_menu.cpp:272–280`), одним спільним helper для read і write, щоб не дублювати код.
        5. Переписати верхню частину `Draw()` для `State::Installing`: компактний статус-рядок, графік R/W з підписами й числами, дві полоски прогресу з лейблами; порахувати нову геометрію так, щоб `m_log_list` не наїжджав і не лишав зайвого порожнього простору. Колір лінії й тексту `R`/`W` брати з теми (додати нові `ThemeEntryID`, якщо підходящих кольорів синій/зелений ще немає, або перевикористати наявні info/success-подібні кольори, якщо це не суперечить темі).
        6. Перевірити, що графік коректно поводиться на дуже малих і дуже великих швидкостях (без NaN/ділення на нуль при size==0), і що загальна полоска прогресу коректно рахує суму по чергою з кількох пакетів, включно з випадком, коли останній пакет менший за середній (не переповнити 100%).
        7. Додати en+uk i18n для нових лейблів (`Current`, `Overall`, якщо потрібні — `R`/`W` можна лишити без перекладу як умовні позначки); підняти версію в `sphaira/CMakeLists.txt`, додати розділ у `walkthrough.md`; збірку виконує Агент 1 у WSL.
    - **Перевірка:** під час DBI install видно графік із двома кольоровими лініями (read і write) і поточні числові MiB/s кожної біля графіка великим шрифтом; шапка не виглядає порожньою чи перевантаженою текстом; видно дві полоски прогресу — поточного пакета й усієї черги — обидві коректно ростуть і не перевищують 100%; журнал знизу й досі прокручується touch без регресії; MTP/Web/File install-шляхи, що не використовують DBI `InstallProgress`, не змінили поведінку.
- [ ] Зробити рядок журналу install-черги динамічним: одна строка на пакет, що переписується `Starting → Installed/Failed/Cancelled`, а не дві окремі строки (пункт із ручного тесту після id 50/54) <!-- id: 55 -->
    - **Проблема з ручного тесту:** для кожного пакета зараз пишуться два окремі рядки журналу — `AddLog("Starting: " + name)` перед інсталяцією (`dbi_menu.cpp:389`) і `AddLog("Installed/Cancelled/Failed: " + name)` після (`dbi_menu.cpp:404–406`). Обидва лишаються в списку назавжди. Оскільки в 99%+ випадків помилок немає (журнал підтверджено чистий у ручному тесті), другий рядок майже завжди лише дублює перший з іншим словом і вдвічі роздуває журнал без нової інформації. Інтерфейс уже динамічний (графік/полоски оновлюються на місці), тому для одного пакета природно мати один рядок, що змінює текст/колір за станом, а не накопичувати історію переходів.
    - **Кроки:**
        1. Додати в `Menu` (dbi_menu.hpp) поле для індексу "живого" рядка журналу поточного пакета, наприклад `s64 m_active_log_index{-1};`.
        2. Розширити `AddLog` (dbi_menu.cpp:505) або додати сусідній метод `Menu::AddOrUpdateLog(s64& index_ref, const std::string& text)`, який: якщо `index_ref` валідний і ще вказує в межах `m_log` на той самий слот (не був витіснений через `MAX_LOG_LINES`), перезаписує `m_log[index_ref] = text` на місці; інакше додає новий рядок звичайним `AddLog()` і записує `index_ref = m_log.size() - 1`. Врахувати зсув індексів при `m_log.erase(m_log.begin())` так само, як зараз коригується `m_log_index` (dbi_menu.cpp:509–510) — якщо `m_active_log_index` посилається на елемент, що видаляється або зсувається, оновити його теж.
        3. У циклі встановлення (dbi_menu.cpp:389,404–406): при старті пакета скинути `m_active_log_index = -1` і викликати `AddOrUpdateLog(m_active_log_index, "Installing: " + name)`; при завершенні викликати той самий `AddOrUpdateLog(m_active_log_index, ...)` з фінальним текстом (`Installed`/`Cancelled`/`Failed: ... (код)`) замість окремого `AddLog`.
        4. Дати "живому" рядку інший колір під час встановлення (напр. `ThemeEntryID_TEXT_INFO`, поки триває) і колір за результатом після завершення (звичайний текст / error-колір для Failed) — потрібно передавати або зберігати статус разом із текстом (можна розширити `m_log` з `std::vector<std::string>` на невеликий struct `{text, LogKind kind}` замість голого рядка, якщо колір по типу зараз ніяк не диференціюється).
        5. Перевірити touch-scroll `m_log_list`, коли рядок оновлюється, а користувач прогорнув журнал вище: перезапис існуючого рядка на місці не повинен смикати позицію скролу (на відміну від append, який це вже коректно обробляє через `follow_tail`).
        6. Підняти версію в `sphaira/CMakeLists.txt`, додати розділ у `walkthrough.md`. Нових i18n-рядків не додається — самі тексти `Installing:`/`Installed:`/`Failed:`/`Cancelled:` вже локалізовані.
    - **Перевірка:** під час install кожен пакет займає рівно один рядок журналу, який видно, як текст і колір міняються з `Installing` на фінальний стан без появи другого рядка; при помилці пакета видно причину в тому ж рядку; ручний скрол журналу під час активного install не збивається кожним переходом стану.
- [ ] Покращити наочність екрана `ReviewQueue` (вибір пакетів для install): жирний шрифт назв, помітніші розмір/носій установки, сегментована діаграма зайнятого місця з підсвіткою вибраної гри (продовження id 50) <!-- id: 56 -->
    - **Проблема з ручного тесту:** у списку черги (`dbi_menu.cpp:247–268`) назва файлу, розмір, тип носія й позначка Exact/Estimate мають однаковий дрібний вигляд — усе намальовано одним некритним шрифтом майже однакового розміру й кольору (`ThemeEntryID_TEXT`/`ThemeEntryID_TEXT_INFO`), тому очам нема за що зачепитися: назва не виділяється серед метаданих, а розмір і цільовий носій — найважливіші перед підтвердженням install — не привертають увагу першими.
    - **Підтверджений технічний контекст:** `gfx::drawTextArgs` у цьому проєкті приймає розмір шрифту окремим параметром NanoVG (`nvgFontSize`), а жирність шрифту залежить від того, який `NVGcontext`-шрифт зареєстровано під `vg` (звичайний і жирний варіант, якщо він підключений через `nvgCreateFont`) — перевірити в `gfx`/`Application`, чи вже є зареєстрований bold-шрифт (наприклад для заголовків меню) і як на нього перемикатися (`nvgFontFaceId`/окремий helper), перш ніж додавати новий font asset.
    - У шапці `MenuBase`/`menu_base.cpp:249–277` вже є готовий патерн "track + fill" бар для NAND/SD із кольором за заповненістю (`draw_storage_bar`) — саме цей візуальний мову (track/fill, кольори за порогами) варто повторно використати для сегментованої діаграми в черзі, а не вигадувати новий стиль.
    - **UX-рішення:**
        1. У кожному рядку черги виділити назву пакета жирним і більшим кеглем (наприклад +2–4pt і bold-шрифт, якщо доступний, інакше — яскравіший/контрастніший колір теми) відносно другого рядка метаданих; розмір install (`Install size`) і обраний носій (`Target`) підняти в окремий, візуально акцентований блок (більший кегль або кольоровий бейдж кшталт "→ microSD"/"→ System memory"), а не рівний за вагою текст поруч із `Package size`/`Exact`/`Estimate`.
        2. Додати дві горизонтальні сегментовані діаграми зайнятого місця під зведенням угорі екрана (де зараз рядки `Targets/Selected/Required` і `microSD free/System memory free/Reserve`, dbi_menu.cpp:237–245) — одна для microSD, одна для System memory. Кожна діаграма: track = загальний обсяг носія (`sd_total`/`nand_total`, GetPolledData()), базовий used-сегмент = вже зайняте місце (як у header, тим самим кольором за порогом заповненості), далі послідовні сегменти-"шматочки" для кожного вибраного пакета, чий `entry.target` (з урахуванням `Auto → suggested_sd`) відповідає цьому носію, шириною пропорційно `analysis.install_size`.
        3. Сегмент пакета, на якому зараз стоїть фокус списку (`index == m_index`), підсвітити (яскравіша заливка/обвідка) на відповідній діаграмі; коли фокус на пакеті іншого носія, на "своїй" діаграмі підсвітки немає — це і є touch/D-pad еквівалент "наведення" на Switch (миші немає, тому "hover" = поточний фокус рядка, а не позиція курсора).
        4. Діаграми мають динамічно перебудовуватись при: зміні вибору пакета (X), зміні цільового носія конкретного пакета (R3 циклічний Auto/microSD/System memory) і зміні фокусу списку (для підсвітки) — increase/decrease сегментів без миготіння, оскільки все одно перемальовується щокадру з `m_queue`.
    - **Кроки:**
        1. Перевірити наявність bold-шрифту в проєкті (`grep -rn "nvgCreateFont" sphaira/source`); якщо є лише один regular font, додати bold-варіант тим самим шляхом (romfs font asset) або, як дешевший перший крок, підсилити ієрархію лише кеглем/кольором без нового font asset — вирішити на основі того, що реально підключено.
        2. Переробити рядок пакета в `m_list->Draw` (dbi_menu.cpp:247–268): назва — великий/жирний перший рядок; другий рядок розбити на два візуально розділені блоки (розмір і носій) замість суцільного `"%s: %s    %s: %s (%s)    %s: %s"`.
        3. Написати спільний helper `DrawStorageSegments(vg, theme, Vec4 rect, s64 total, s64 used_base, std::span<Segment> segments, s64 highlighted_index)` (Segment = {size, colour}), що малює track, базовий used, послідовні сегменти з розділювачами в 1px і підсвічує вибраний; винести його так, щоб потенційно перевикористати логіку `draw_storage_bar` з `menu_base.cpp`, не дублюючи кольори порогів.
        4. У `Menu::Draw` для `State::ReviewQueue` під існуючим текстовим зведенням (після рядка `GetY()+36`) додати виклик двох діаграм (SD/NAND) і зсунути початок `m_list` (`GetY()+...`) під нову висоту; підготувати масив сегментів із `m_queue`, фільтруючи за `entry.selected && R_SUCCEEDED(analysis_result)` і резолвлячи `Auto → suggested_sd`.
        5. Перевірити overflow/edge-cases: сумарний обсяг вибраних перевищує вільне місце (сегменти виходять за track — обрізати/показати попередження, вже наявне за текстом low-space), 0 вибраних пакетів (діаграма показує лише used, без сегментів), дуже маленький пакет (мінімальна видима ширина сегмента, наприклад 2px, щоб не зникав).
        6. Підняти версію в `sphaira/CMakeLists.txt`, додати розділ у `walkthrough.md`; нових i18n-рядків не очікується, якщо не додаються нові підписи.
    - **Перевірка:** назва пакета в черзі виразно домінує над метаданими; розмір install і носій видно з першого погляду, не змішані з `Exact`/`Estimate`; під зведенням видно дві діаграми (SD/NAND) із базовим зайнятим місцем і сегментами вибраних пакетів пропорційно розміру; перемикання фокусу списку підсвічує відповідний сегмент на правильній діаграмі; зміна цільового носія пакета (R3) і зміна вибору (X) миттєво перебудовують сегменти без збою макета; для 0 вибраних пакетів діаграма не ламається.
- [ ] Журнал install-черги не прокручується автоматично до низу при появі нових рядків (продовження id 50/54/55, скарга з ручного тесту) <!-- id: 57 -->
    - **Проблема з ручного тесту:** під час install у DBI-черзі журнал (`m_log_list`) додає нові рядки знизу, але видима область не прокручується — користувач бачить старі рядки вгорі списку й мусить сам прокручувати пальцем/стіком до низу, щоб побачити щойно доданий рядок.
    - **Підтверджений технічний контекст:**
        - `Menu::AddLog()` (`dbi_menu.cpp:505–514`) рахує `follow_tail = m_log.empty() || m_log_index >= m_log.size() - 1` ДО додавання рядка і, якщо true, підтягує `m_log_index` до нового останнього індексу — тобто логічний "хвіст" (індекс) коректно рухається. Викликається з робочого потоку (`SCOPED_MUTEX`), не з головного.
        - Але `m_log_index` — це лише позиція фокусу/курсора для `List::OnUpdate`, а не скрол `List`. `List::OnUpdateGrid` (`list.cpp:298+`) змінює внутрішній `m_yoff` лише у відповідь на реальний D-pad/touch-інпут (`ScrollDown`/`ScrollUp`/`ScrollToEnd` викликаються тільки з гілок `controller->GotDown(...)`), а не автоматично слідом за зміною переданого ззовні `index`. Тобто саме по собі оновлення `m_log_index` в `AddLog()` ніяк не рухає `m_yoff` — звідси видима відсутність автоскролу, хоча `m_log_index` формально вже вказує на останній рядок.
        - `Menu::Update()` (`dbi_menu.cpp:197–202`) щокадру викликає `m_log_list->OnUpdate(controller, touch, m_log_index, m_log.size(), ...)` — це єдине місце на головному потоці, де безпечно можна поворухнути `m_log_list` (сам `List` не потокобезпечний і має малюватись/оновлюватись лише з головного потоку).
        - `List::ScrollToEnd(s64& index, s64 count) -> bool` (`list.cpp:197`) — публічний метод, безумовно виставляє `index = count - 1` і рахує коректний `m_yoff` для нижнього краю; саме його треба викликати з `Update()`, а не з `AddLog()`.
    - **Рішення:** у `dbi_menu.hpp` додати `s64 m_log_last_seen_size{};`. У `Menu::Update()` одразу після існуючого `m_log_list->OnUpdate(...)` (яке вже обробило можливий ручний скрол цього кадру й могло змінити `m_log_index`) порівняти поточний `m_log.size()` з `m_log_last_seen_size`; якщо розмір зріс — оновити `m_log_last_seen_size` і, тільки якщо `m_log_index` після `OnUpdate()` усе ще вказує на (старий) хвіст (`m_log_index >= log_size - 1`), викликати `m_log_list->ScrollToEnd(m_log_index, log_size)`. Порядок важливий: ручний скрол користувача в цьому ж кадрі (через `OnUpdate`) має пріоритет і не повинен перебиватись автоскролом.
    - **Перевірка:** під час активного install журнал сам прокручується до щойно доданого рядка без дій користувача; якщо користувач прогорнув журнал вгору вручну, нові рядки, що приходять, більше не збивають його позицію (це вже мало працювати за задумом id 50, але візуально не було помітно через відсутній автоскрол); після ручного скролу вниз аж до останнього рядка автоскрол відновлюється.
- [ ] З `Summary`/`Cancelled` install-черги B веде одразу в Tools замість повернення до `ReviewQueue` для до-встановлення (продовження id 50, скарга з ручного тесту) <!-- id: 58 -->
    - **Проблема з ручного тесту:** після завершення встановлення користувач хоче повернутися до списку надісланих DBI-пакетів і встановити щось із того, що раніше зняв галочкою (не вибрав) — але кнопка B на екрані `Summary`/`Cancelled` одразу закриває весь `dbi::Menu` і викидає в `Tools`, без проміжного кроку.
    - **Підтверджений технічний контекст:** `Menu::UpdateActions()` (`dbi_menu.cpp:174–175`) для `state == State::Summary || state == State::Cancelled || state == State::Failed` призначає `Button::B → Action{"Back"_i18n, [this](){ SetPop(); }}`, що знімає весь `dbi::Menu` зі стека віджетів — повернення можливе лише повторним підключенням DBI-сесії з нуля (новий `Menu`, нове `WaitForConnection()`), а НЕ до вже отриманого списку `m_queue` з поточної USB-сесії.
    - **Питання, яке потрібно вирішити перед реалізацією:** USB DBI-сесія (`m_usb_source`) після `Install selected` могла вже відповісти клієнту (`dbibackend-qt`) фінальним статусом/`Exit`, тому "повернення до ReviewQueue" — це не просто зміна `m_state`, а питання, чи можна повторно запропонувати той самий `m_queue` (уже проаналізовані пакети, знята позначка `selected`) БЕЗ нового round-trip по USB, чи потрібен новий цикл `Analysing` для щойно позначених пакетів. Уточнити протокол з `dbibackend-qt` (чи прийме він повторний `Install selected` у межах тієї ж сесії) перед тим, як міняти `UpdateActions()`.
    - **Мінімальне рішення (якщо повторний install у тій же сесії неможливий):** на `Summary`/`Cancelled` замінити `Back` на явну дію (наприклад `X`) "Select more" / "До-встановити", яка скидає `selected` лише для НЕ встановлених пакетів і повертає `m_state = State::ReviewQueue`, залишаючи `B` для повного виходу з `dbi::Menu` (перейменувавши на щось на кшталт `Exit`, щоб не плутати з поверненням).
    - **Перевірка:** з екрана `Summary` є спосіб повернутись до списку пакетів поточної USB-сесії й вибрати ще щось для встановлення без повного виходу з `dbi::Menu` в `Tools`; повний вихід (`B`/окрема дія) як і раніше коректно завершує USB-сесію й повертає MTP, якщо він був увімкнений.
- [ ] Повідомлення очікування підключення ПК не вміщується в екран (продовження id 50, скарга з ручного тесту) <!-- id: 59 -->
    - **Проблема з ручного тесту:** текст `"Waiting for PC connection. Connect the USB cable and make sure the console is detected."` (і сусідні повідомлення для `WaitingForList`/`Analysing`) виходить за межі екрана.
    - **Підтверджений технічний контекст:** `Menu::Draw()` (`dbi_menu.cpp:209–217`) малює ці рядки одним викликом `gfx::drawTextArgs(vg, SCREEN_WIDTH/2, SCREEN_HEIGHT/2, 30.f, NVG_ALIGN_CENTER|NVG_ALIGN_MIDDLE, ..., text.c_str())` — це один рядок без переносу (на відміну від `gfx::drawTextBox`, який в проєкті вже використовується для багаторядкового тексту, напр. в `OptionBox`/`Sidebar` info-блоках). При кеглі 30px довге англійське речення в один рядок ширше за `SCREEN_WIDTH`, тому виходить за краї.
    - **Рішення:** замінити виклик на `gfx::drawTextBox(...)` (або еквівалент з переносом рядків) з обмеженням ширини (наприклад `SCREEN_WIDTH - 2*pad`) і, за потреби, трохи зменшити кегль для довшого варіанту (`WaitingForUsb`); перевірити центрування по вертикалі багаторядкового блоку (`drawTextBox`, на відміну від `drawTextArgs` з `NVG_ALIGN_MIDDLE`, може рахувати вертикальний якір інакше — звірити з тим, як це вже зроблено в `Sidebar`-info-блоках).
    - **Перевірка:** усі три повідомлення (`WaitingForUsb`/`WaitingForList`/`Analysing`) повністю видимі й центровані на екрані без обрізання країв та без накладання на іншу розмітку.
- [ ] Меню "Games" (`game_menu.cpp`): перенесення ігор NAND ↔ SD із наочним індикатором зайнятого/вільного місця, сортування за носієм <!-- id: 60 -->
    - **Побажання користувача:** у списку встановлених ігор (те саме меню `ui::menu::game::Menu`, яке відкривається з `Tools → Games`) потрібна можливість переносити гру між System memory (NAND) і microSD прямо звідти, з наочним відображенням зайнятого/вільного місця обох носіїв — за аналогією з вимогою до id 56 (сегментована діаграма зайнятого місця в install-черзі DBI, `menu_base.cpp` вже має готовий патерн `draw_storage_bar` track+fill з кольором за порогом заповненості, який варто перевикористати, а не вигадувати новий стиль). Додатково — сортування списку ігор за тим, на якому носії стоїть гра (NAND / SD).
    - **Підтверджений технічний контекст:**
        - `game_menu.hpp`: `struct Entry` (`~14–30`) наразі НЕ зберігає `storageID` — тільки `app_id`, `lang`, `image` тощо. `enum SortType` (`~32–36`): `Updated`/`Alphabetical`/`Publisher`, носія серед варіантів немає.
        - Реальне сховище контенту читається на рівні окремих content-записів, а не гри в цілому: `BuildContentEntry()` (`game_menu.cpp:263–305`) для кожного `NsApplicationContentMetaStatus` бере `status.storageID` (`NcmStorageId_BuiltInUser`/`NcmStorageId_SdCard`/`NcmStorageId_GameCard`) і відповідний `title::GetNcmCs(status.storageID)`/`GetNcmDb(...)`. **Важливо:** Base/Update/DLC однієї гри можуть фізично лежати на РІЗНИХ носіях одночасно (наприклад Base на NAND, DLC на SD) — "носій гри" для сортування/бейджа має бути визначеною політикою (напр. носій Base-запису, або "мішаний", якщо records розходяться), а не гарантовано єдиним значенням.
        - У кодовій базі **немає** готової функції "перемістити встановлений контент між NcmStorageId" (перевірено: `grep` на `nsMoveApplication`/`ncmContentStorageMove` нічого не знаходить). Є лише `NcmStorageId_SdCard`/`NcmStorageId_BuiltInUser` як цільове сховище при *встановленні* (`yati.cpp:936`, `app.cpp:1060`), не для вже встановленого контенту. Офіційний шлях на Switch — це копіювання NCA (`ncm::GetContentIdFromStr`/`ncmContentStorageReadContentIdFile`, вже використовується в `game_menu.cpp:82` для читання) у cs цільового сховища, реєстрація в `NcmContentMetaDatabase` цільового сховища, видалення зі старого — це найризикованіша й найважча частина задачі (аналогічно до застереження id 37 "найскладніша задача списку"); варто дослідити, чи є в Atmosphere/libnx офіційний high-level виклик перед тим, як писати низькорівневий копі-і-перереєстрací вручну.
        - Вільне/зайняте місце NAND і SD вже читається деінде застосунком (напр. `MenuBase`/`menu_base.cpp` header для головного меню, `dbi_menu.cpp` зведення `microSD free`/`System memory free`) — для індикатора в Games можна перевикористати той самий джерело даних (`fs::FsNativeContentStorage(...).GetTotalSpace/GetFreeSpace`, вже бачено в `haze_helper.cpp:766,773`).
    - **Кроки (орієнтовно, уточнити при плануванні реалізації):**
        1. Дослідити офіційний спосіб переносу встановленого контенту між storage на Switch (Atmosphere/libnx) — чи є атомарний high-level API, чи доведеться писати ручний copy+register+unregister по NCA.
        2. Розширити `Entry`/`ContentInfoEntry` полем "носій" (або мапою носій → розмір за типом контенту), розрахованим по всіх `NsApplicationContentMetaStatus` гри.
        3. Додати `SortType_Storage` (або окремий фільтр NAND/SD) в `enum SortType` і відповідний UI-пункт сортування поруч із наявними `Updated`/`Alphabetical`/`Publisher`.
        4. Додати дію переносу (наприклад через `SidebarEntryCallback` поруч із наявним `Delete`, `game_menu.cpp:607–617`) з підтвердженням, прогрес-баром на час копіювання великого контенту та перевіркою вільного місця на цілі ДО старту (аналогічно перевіркам low-space в id 50/56).
        5. Додати індикатор зайнятого/вільного місця обох носіїв, перевикористовуючи `draw_storage_bar`-стиль track+fill з `menu_base.cpp`.
        6. i18n, версія, walkthrough — за загальною конвенцією.
    - **Перевірка:** з меню Games можна перенести гру (або окремо Base/Update/DLC, якщо політика "носія" це розрізняє) з NAND на SD і назад, з наочним баром зайнятого місця обох носіїв до/після; сортування за носієм коректно групує NAND і SD; перенос при нестачі місця на цілі блокується з поясненням, а не падає посеред копіювання.
- [ ] Меню "Games": замінити запуск гри по A на інформаційний екран (назва, мови, метадані — за зразком DBI) <!-- id: 61 -->
    - **Побажання користувача:** зараз `Button::A` в списку встановлених ігор одразу запускає гру (`game_menu.cpp:472`, `Action{"Launch"_i18n, ...}`). Користувач хоче, щоб A замість запуску відкривав інформаційний екран гри — за зразком того, що показує клієнт DBI (Title ID, версія, мови, розмір, метадані контенту тощо). Користувач обіцяв надіслати скріншот DBI пізніше як точний референс полів — **до отримання скріншота конкретний перелік полів не финалізувати**, орієнтуватись на дані, які вже збираються в цьому ж файлі.
    - **Підтверджений технічний контекст:** дані для такого екрана здебільшого вже читаються в `game_menu.cpp`, лишається їх показати замість запуску:
        - `Entry`/`NacpLanguageEntry` (`lang.name`/`lang.author` — `GetName()`/`GetAuthor()`, `game_menu.hpp:23–29`) — назва й видавець уже є; повний `NacpLanguageEntry`, ймовірно, містить більше мовних полів, які зараз не виводяться.
        - `BuildContentEntry()`/`ContentInfoEntry` (`game_menu.cpp:263–305`) уже збирає per-title-id список content (Base/Update/DLC), `status.meta_type`, `status.storageID`, `status.version`, розмір через `ncmContentInfoSizeToU64` (`~332`); рядок `"Type: %s Storage: %s [%016lX][v%u]"` (`game_menu.cpp:566`) уже показує частину цього в іншому місці UI — можна взяти за основу форматування.
        - Треба вирішити: чи запуск гри (Launch) залишається доступним з цього нового екрана (напр. окрема кнопка/дія всередині інфо-екрана), чи повністю прибирається з A і переноситься на інший button/дію в списку — уточнити з користувачем при плануванні реалізації, у побажанні прямо сказано "не запускати гру по A", але без явної вказівки, куди дінеться сам запуск.
    - **Кроки (орієнтовно):**
        1. Дочекатись скріншота DBI від користувача для фінального переліку полів і візуального стилю.
        2. Замінити `Action{"Launch", ...}` на `Button::A` новим інфо-екраном/Sidebar; вирішити долю самого запуску гри (окрема кнопка чи дія).
        3. Зібрати повний набір полів (мови з `NacpLanguageEntry`, per-content Base/Update/DLC з розмірами й носієм, Title ID/версія) в один екран.
        4. i18n, версія, walkthrough.
    - **Перевірка:** A відкриває інформаційний екран гри з коректними даними (мови, Title ID, версія, розмір і носій кожного типу контенту); гру, як і раніше, можна запустити (шляхом, узгодженим із користувачем).
- [ ] Меню "Games": значки на іконці гри — наявність Base/Update/DLC/LayeredFS (mods) <!-- id: 62 -->
    - **Побажання користувача:** у сітці ігор на іконці кожної гри показувати маленькі значки/галочки, які означають, що встановлено: базу (Base), оновлення (Update), DLC, і чи активний LayeredFS (моди/патчі через layered filesystem romfs/exefs override).
    - **Підтверджений технічний контекст:**
        - Base/Update/DLC: дані вже збираються через `BuildContentEntry()`/`NsApplicationContentMetaStatus.meta_type` (`game_menu.cpp:263+`) — по суті вже є все необхідне, щоб для кожної гри знати, які з трьох типів встановлені; питання лише в кешуванні цієї інформації на рівні `Entry` сітки (зараз, схоже, рахується на вимогу/деінде, а не зберігається постійно в `Entry` для швидкого малювання бейджа в `Draw()`), щоб не робити важкі NCM-запити на кожен кадр для кожної видимої плитки.
        - LayeredFS: у цьому файлі прямих згадок `LayeredFS`/`romfs`/`exefs` override не знайдено (`grep` на `game_menu.cpp` порожній) — джерело правди для "чи активний LayeredFS для цієї гри" потрібно ще визначити (типова конвенція Atmosphere — `/atmosphere/contents/<TID>/romfs`/`exefs_patches`/`romfs_patches`; перевірити, чи є вже десь у проєкті готова перевірка наявності таких тек, напр. поруч з `cheats/cheats_lookup.cpp`, де вже є робота з `NcmStorageId`/title-специфічними даними).
        - Малювання плитки гри — `game_menu.cpp` (сітка), треба знайти конкретний малюючий блок (`DrawEntry`/аналог) і додати шар маленьких іконок/бейджів у кутку прев'ю, не ламаючи наявний layout (назва/автор під іконкою, фокус-рамка).
    - **Кроки (орієнтовно):**
        1. Дослідити, де на диску Atmosphere тримає LayeredFS-патчі для конкретного TID, і чи є в проєкті вже готова перевірка (`fs::DirExists`-подібна) для цього шляху.
        2. Розширити `Entry` полями-прапорцями (`has_base`/`has_update`/`has_dlc`/`has_layeredfs`), заповнюваними один раз при скануванні бібліотеки (там же, де зараз збирається `Entry`), а не на кожен кадр.
        3. Додати маленькі NanoVG/glyph-іконки в кутку плитки (за зразком чекбоксів/бейджів, вже використаних в install-черзі DBI, id 50) для кожного прапорця, що true.
        4. Перевірити читабельність на дрібних плитках (Icon/Grid layout) і в HbMenu-режимі, де плитка велика — бейджі не повинні губитись чи, навпаки, перекривати назву/автора.
        5. i18n (підписи для tooltip/легенди, якщо додається), версія, walkthrough.
    - **Перевірка:** на кожній плитці видно, чи встановлено Base/Update/DLC/LayeredFS для цієї гри, без помітного впливу на продуктивність прокрутки сітки (дані прапорців кешовані, не рахуються щокадру); бейджі не перекривають існуючий текст/фокус-рамку в жодному з layout-режимів.
- [x] Переробити структуру збереження сейвів: замість групування за типом (Account/Bcat -> ігри -> бекапи) групувати за грою (гра -> Account/Bcat/інші типи -> бекапи), відповідно оновити й відображення в МТП (пункт 22) — реалізовано у v0.13.148 <!-- id: 38 -->
    - **Контекст:**
        - Шлях бекапа будує ЄДИНА функція `BuildSaveBasePath` (`sphaira/source/ui/menus/save_menu.cpp:331–342`): `<root>/<GetSaveFolder(type)>/<ім'я або TID>`, де `GetSaveFolder` → "Save" / "Save BCAT" / "Save Device" / "Save Cache" / "Save System"... (`save_menu.cpp:100–111`).
        - Усі використання (усі в save_menu.cpp): `1439` (автосинк, remote-шлях `sphaira-saves/...`), `1471` (`FindLatestBackupPath` — вже сканує 2 варіанти: ім'я та TID), `1549` (базовий шлях бекапа/перегляду), `1915` і `1921` (ручний синк: local + remote). Тобто зміна формули автоматично міняє і локальні бекапи, і структуру на WebDAV.
    - **Нова структура:**
        - Account/Bcat/Device/Cache/Temporary: `<root>/<GameName>/<Підпапка типу>`, де підпапка: Account → "Account", Bcat → "BCAT", Device → "Device", Cache → "Cache", Temporary → "Temporary".
        - System/SystemBcat: залишити стару схему (`<root>/Save System/<id>`) — "гри" немає, ламати нічого не треба.
    - **Кроки:**
        1. Додати `GetSaveTypeSubdir(u8 data_type)` (нові короткі назви вище) поряд із `GetSaveFolder` (старий лишити для legacy-шляхів).
        2. Переписати `BuildSaveBasePath`: для System-типів — стара формула; для решти — `AppendPath(AppendPath(backup_root, name), GetSaveTypeSubdir(type))`, де `name` — та сама логіка, що зараз (BuildSaveName або `%016lX` TID при force_id_path/corrupted).
        3. Зворотна сумісність читання: додати `BuildSaveBasePathLegacy` (точна копія старої формули). У `FindLatestBackupPath` (1468–1489) розширити перебір із 2 до 4 колекцій: нова(ім'я), нова(TID), стара(ім'я), стара(TID) — нові шляхи першими, щоб свіжі бекапи мали пріоритет.
        4. Перевірити місце `1549` (перегляд/відновлення бекапів): якщо там будується шлях для браузингу — показувати новий шлях, а якщо тека порожня/відсутня — падати назад на legacy-шлях.
        5. Ручний синк (1915–1921): запис — лише нова структура. Старі файли на WebDAV залишаться на старих шляхах і синк заллє їх заново в нову структуру — зафіксувати це у walkthrough як відому/очікувану поведінку (опційно можна додати перевірку legacy remote-шляху, але це не обов'язково).
        6. Відображення в MTP: стосується задачі id 37 — там ієрархія вже "гра → тип", отже узгоджено; якщо id 37 ще не зроблена, тут нічого не робити.
        7. i18n не потрібен (назви папок на диску не перекладаються). Версія, walkthrough.
    - **Перевірка:** новий бекап лягає в `<root>/<Гра>/Account/...zip`; відновлення бачить і нові, і старі бекапи (створені до зміни); автосинк вантажить у `sphaira-saves/<Гра>/<Тип>/`; системні сейви бекапляться, як раніше.
    - **Реалізовано:** для Account/BCAT/Device/Cache/Temporary запис локальних і WebDAV-бекапів переведено на `<root>/<GameName або TitleID>/<Type>`; System/SystemBcat зберігають стару схему. Пошук відновлення перевіряє нові шляхи за назвою й Title ID, потім обидва legacy-варіанти. Старі локальні та WebDAV-каталоги не мігруються: ручна синхронізація працює лише з новою схемою, а legacy-шляхи читає тільки Restore.
- [x] Сумісність сейв-бекапів із DBI: читати бекапи DBI (`/switch/DBI/saves`) при відновленні та писати власні несистемні бекапи у форматі й розташуванні DBI — реалізовано у v0.13.149 <!-- id: 51 -->
    - **Контекст:** формат DBI розібрано з реальних бекапів користувача: `/switch/DBI/saves/<гра>/<YYYYMMDD>/<TID>_<A|B|D|C|T>_<YYYYMMDDHHMMSS>_<index>.zip`; всередині — файли сейву з абсолютними шляхами + `.dbi_save_info.ini` + `.dbi_save_extra` (сирі 512 байт `FsSaveDataExtraData`). Назва теки гри — лише ASCII-літери/цифри/пробіли.
    - **Реалізовано:** несистемні бекапи sphaira пишуться у DBI-форматі (без `.nx_save_meta.bin`; його заміняє `.dbi_save_extra`). Restore збирає кандидатів із DBI, нової та legacy структур sphaira і вибирає найновіший за таймстемпом з імені файла; для Account/Cache профіль/індекс звіряється з `.dbi_save_extra`. Розпакування DBI-ZIP пропускає метафайли DBI і бере точні data/journal size з `.dbi_save_extra`. Ручний sync враховує локальні DBI-файли; завантаження з WebDAV із DBI-іменами кладуться в DBI-структуру. System/SystemBcat — без змін. Структура `<root>/<гра>/<тип>` (id 38) стала read-only-legacy.
    - **Перевірка:** бекап sphaira видно й відновлюється у DBI; бекап DBI відновлюється у sphaira (з коректним профілем для Account); старі sphaira-бекапи досі читаються; sync працює в обидва боки через `sphaira-saves/<гра>/<тип>/`.

## Рекомендований порядок виконання невиконаних задач

### Поточний етап — сейви та DBI/MTP backend

> Окремої невиконаної задачі саме для протоколу **DBI Backend** у плані немає: базовий DBI0 та `PC Install (USB)` уже реалізовані. До цього етапу належать незавершені задачі MTP/backend (`haze_helper.cpp`) та сейви. Їх виконуємо в наведеному порядку; сторонні UI-доробки не вклинюємо між ними.

1. ~~**id 38** (структура бекапів `гра → тип`)~~ — виконано у v0.13.148.
2. ~~**id 50** (DBI Install queue)~~ — виконано у v0.13.153.
3. ~~**id 49** (зрозуміла WebDAV-синхронізація сейвів)~~ — виконано у v0.13.154, розміщення виправлено у v0.13.155.
4. ~~**id 52** (вибір і відображення папки сейв-бекапів)~~ — виконано у v0.13.155 (довгий шлях — скрол, не wrap; див. примітку в описі задачі).
5. **id 43** (попап прогресу для звичайного MTP-копіювання) — **перша наступна задача**: завершує видимість звичайних передач у USB стеку, не торкається схеми сейвів.
6. **id 31** (налаштування сховищ MTP) — створює таблицю реєстрації MTP-дисків, їхню видимість і назви; це обов’язкова основа для додавання `Saves` без дублювання `haze::Init()`.
7. **id 37** (диск `Saves` у MTP) — завершує поточний етап: спирається одночасно на id 38 (та сама ієрархія `гра → тип`) і id 31 (таблиця сховищ + тумблер). Робити лише після обох.

### Після поточного етапу — незалежні UX-задачі

7. **id 47** (папка WebDAV у Network + дружнє введення адреси) — UX для конфігурації WebDAV, не змінює синхронізацію.
8. **id 48** (сенсор головного Settings) — ізольований bugfix viewport двох колонок, не торкається спільного `List` і робочого Tools.
9. **id 44 + id 46** (зрозумілі стани, описи й геометрія Module Manager) — спільний UX-пакет після вже виконаного id 45.
10. **id 54** (графік R/W швидкості й друга полоска прогресу на екрані install черги) — чистий UI-доробок над уже реалізованим id 50, не змінює протокол DBI/Yati окрім нового геттера read-прогресу; можна робити в будь-який момент після id 50.
11. **id 55** (один динамічний рядок журналу на пакет замість Starting/Installed окремо) — незалежний від id 54, можна робити паралельно чи в будь-якому порядку з ним.
12. **id 56** (наочність ReviewQueue: жирні назви, сегментована діаграма зайнятого місця) — теж незалежний UI-доробок над id 50; логічно робити разом із id 55, оскільки обидва торкаються того самого екрана черги.

Спільні конвенції для кожної задачі: нові рядки UI → `_i18n` + ключі в `assets/romfs/i18n/en.json` і `uk.json`; після завершення — підняти версію в `sphaira/CMakeLists.txt` і додати розділ у `walkthrough.md`; збірку виконує Агент 1 (WSL), реалізатор збірку сам не запускає.


