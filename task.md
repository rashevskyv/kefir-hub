# Список завдань: Контекстне меню джерел, тестування з'єднання, копіювання URL, синхронізація джерел та збереження позиції курсору (v0.13.236)

- [x] **Підготовка та налаштування**:
  - [x] Створити plan та task в корені програми українською
  - [x] Отримати погодження плану від користувача та відповідь щодо потреби автоматичної збірки NRO
- [x] **Реалізація змін**:
  - [x] Додати допоміжну функцію `IsUrlLike` у `settings_menu.cpp` та `filebrowser.cpp`
  - [x] Змінити контекстне меню джерела у `settings_menu.cpp` (замінити `PopupList` на `Sidebar`)
  - [x] Реалізувати функцію тестування з'єднання `TestConnection` та додати відповідні пункти у контекстне меню та `SourceEditMenu` у `settings_menu.cpp`
  - [x] Оновити `AddNetworkLocationInteractive` у `filebrowser.cpp` для автоматичного копіювання URL
  - [x] Відображати всі джерела у списку "Mount" файлового менеджера у `filebrowser.cpp` та виводити повідомлення про відсутність підтримки перегляду для не-SMB джерел
  - [x] Зберегти позицію курсору та зсув скролу при отриманні фокусу в `SettingsMenu` та `SourceEditMenu`
  - [x] Спростити тест підключення для HTTP/WebDAV шляхом використання універсального HEAD запиту (Flag_NoBody) без PROPFIND
  - [x] Вважати тест HTTP з'єднання успішним, якщо отримано будь-який HTTP-код відповіді від 200 до 599
  - [x] Додавати тип протоколу в дужках у верхньому регістрі поруч з ім'ям джерела у меню вибору джерел ("Mount"), списку вибору Sync Location та WebDAV Settings
  - [x] Реалізувати захист від колізій імен: якщо типи джерел однакові — запитувати "Overwrite / Keep Both" (де Keep Both генерує назву з номером в дужках); якщо типи різні — автоматично додавати номер в дужках без запиту
  - [x] Ітерувати версію програми до `0.13.236` у `sphaira/CMakeLists.txt`
- [x] **Верифікація**:
  - [x] Скомпілювати проект за допомогою `make` / `build.sh` у WSL (або за вказівкою користувача)
